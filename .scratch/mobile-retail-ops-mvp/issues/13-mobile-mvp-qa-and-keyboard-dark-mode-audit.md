# 13 — Mobile MVP QA And Keyboard/Dark Mode Audit

**What to build:** verify the complete Mobile Retail Ops MVP workflow across realistic mobile conditions, with special attention to keyboard safety, NativeWind styling, dark mode, safe areas, list performance, and offline state. This ticket should close the loop on the MVP rather than add new product scope.

**Blocked by:** 01 — Owner Signup, OTP, And Business Entry; 02 — First Product Setup Wizard; 03 — Mobile Dashboard Production Snapshot; 04 — Staff Invite And Attendant Onboarding; 05 — Create Sale With Product Variants And Quantity Stepper; 06 — Customer Book During Checkout; 07 — Offline Sale And Inventory Sync Flow; 08 — Sync Failure And Conflict Review UX; 09 — Product Share Link Generation And Management; 10 — Shared Product Web Order Flow; 11 — Shared-Link Order Follow-Up; 12 — Subscription Plan Surface And Upgrade Handoff.

**Status:** in-progress

- [x] Auth, OTP, first product, dashboard, staff invite, create sale, customer book, offline sync, share-link, follow-up, and subscription flows are smoke-tested.
- [x] Signup, OTP, first-product setup, quantity entry, customer entry, checkout, and staff invite forms remain usable when the keyboard is open.
- [x] NativeWind usage is audited so components do not unnecessarily mix className and style on the same native element.
- [x] Light and dark mode are visually checked for primary screens, sheets, banners, and controls.
- [x] Long product and customer lists are tested or manually verified with enough records to confirm virtualized behavior.
- [x] Offline banner placement, sync status states, and conflict messaging are visually checked on compact and larger phone sizes.

## Progress

- Added Expo public URL proof to the production live blocker report. The live checklist now merges the matching mobile env file for real repo env targets, records value-safe `EXPO_PUBLIC_API_URL`, `EXPO_PUBLIC_BASE_URL`, and `EXPO_PUBLIC_WEB_URL` proof, and final evidence validation requires those Expo public values to be `https://ewatrade.com` alongside the root API/storefront public URL keys.
- Tightened final evidence validation for live blocker row linkage. Completed production evidence with remaining blockers now requires every `missingRequired` row in the blocker JSON to match a `missing` item inside the named checklist section with the same label and keys, so a handcrafted blocker summary cannot drift from the detailed checklist rows.
- Tightened final evidence validation for live blocker checklist sections. Completed production evidence with remaining blockers now requires the referenced blocker JSON to include the five live checklist section rows, not just summarized missing rows, so hand-made minimal blocker JSON cannot pass.
- Tightened blocked-release evidence path binding. Completed production evidence with remaining blockers now requires the blocker-report preflight artifact to show `Live checklist report written:` for the exact same JSON path referenced in the Release Gate Summary, so a valid command log and a valid blocker JSON cannot be mixed from separate runs.
- Tightened the production evidence template so blocker-report instructions match the validator. The `Live blocker report` field now tells testers that blocker JSON must come from the production env, match the same run date, and include `missingRequiredCount` plus `publicConfiguredValues` proof when blockers remain; the fixture now guards that wording.
- Tightened final evidence validation for production blocker report origin. Completed production evidence with remaining blockers now requires the referenced live blocker JSON to prove `isProductionEnvTarget: true`, so a report generated from a local/default env cannot be reused for production release evidence even if its prose or URL fields look plausible.
- Tightened final evidence validation for live blocker report counts. Completed hands-on evidence with remaining production blockers now requires the referenced blocker JSON `missingRequiredCount` to match the `missingRequired` row array, so older reports without the count or hand-edited reports with mismatched counts are rejected before release evidence can pass.
- Added a machine-readable missing blocker count to the production live blocker report. `qa:mvp-live-blocker-report:prod` now writes `missingRequiredCount` beside the `missingRequired` rows, fixture coverage asserts the count matches the row array, and the refreshed `/tmp/ewatrade-mvp-live-blockers.json` reports `missingRequiredCount: 22` with the same `https://ewatrade.com` public URL proof.
- Tightened completed evidence validation for production blocker reports. When production evidence still has `Remaining blockers`, the referenced live blocker JSON must now include value-safe `publicConfiguredValues` that prove the API and storefront targets are `https://ewatrade.com`. Fixtures reject both older reports with no URL proof and reports that still point at a stale API host.
- Added value-safe public URL proof to the live blocker report. The JSON written by `qa:mvp-live-blocker-report:prod` now includes `publicConfiguredValues` for allowlisted public URL keys only, so release evidence can prove `API_URL`, `NEXT_PUBLIC_API_URL`, `STOREFRONT_URL`, `NEXT_PUBLIC_STOREFRONT_URL`, and `NEXT_PUBLIC_APP_URL` are set to `https://ewatrade.com` without printing tokens, passwords, emails, share URLs, or evidence paths. The refreshed blocker report still lists 22 missing external live proof values.
- Refreshed production live readiness after the API URL alignment. `qa:mvp-live-env-checklist:prod` and `qa:mvp-live-blocker-report:prod` now confirm the production API/storefront URLs are configured from `.env.production`, with a fresh value-safe blocker report at `/tmp/ewatrade-mvp-live-blockers.json`. `qa:mvp-live-readiness:prod` passes static Google OAuth readiness and fails only on the expected external live proof inputs: Google OAuth live API QA, shared-link live write/email QA, shared-link public preview, and shared-link browser checkout. The blocker report currently lists 22 missing required live values across those four sections.
- Aligned production API URL with the current hosting setup. Production root env, mobile production env, mobile env guard expectations, and the API deploy helper now use `https://ewatrade.com` as the public API URL instead of `https://api.ewatrade.com`. The Expo env attachment checker now also compares public values from `eas env:list --format long` when EAS exposes them, and the live Expo check confirmed production `EXPO_PUBLIC_API_URL` is attached as `https://ewatrade.com`.
- Tightened production target-host validation. When `QA mode` is `production`, the completed hands-on evidence validator now rejects local API or storefront targets such as `localhost`, loopback addresses, `0.0.0.0`, `::1`, and `.local` hosts. Local QA evidence can still use local targets. Fixtures now prove local evidence with localhost targets passes while production evidence with local targets fails.
- Tightened device-smoke command mode consistency. `Device smoke evidence` can still use a written full-device-smoke note, but when it explicitly references `qa:mvp-hands-on-checklist`, the command variant must now match `QA mode`: `:prod` for production and default for local. Fixtures cover both production evidence with a local checklist command and local evidence with a production checklist command.
- Tightened bidirectional QA mode command consistency. Completed hands-on evidence now rejects production `:prod` command variants when `QA mode` is `local`, matching the existing requirement that `production` evidence must use `:prod` commands. Fixtures now prove a complete local evidence note passes with default commands and fails when local evidence references production proof commands.
- Tightened run-context QA mode validation. The completed hands-on evidence validator now accepts only `local` or `production` for `QA mode`, so typos such as `prod` cannot accidentally bypass production-only command checks. Fixtures now reject invalid QA mode values while preserving the `production` path that requires `:prod` proof commands.
- Tightened production command variant validation. When `QA mode` is `production`, completed hands-on evidence now requires the production `:prod` variants for the hands-on checklist, live env checklist, live readiness, full readiness, Google OAuth live run, shared-link live run, shared-link preview, and browser checkout proof commands. Fixtures now reject production evidence that points at local/default command variants, preventing local QA output from being reused as production release evidence.
- Tightened blocked-release preflight evidence. When `Remaining blockers` is not `None`, the completed evidence validator now requires the `qa:mvp-live-blocker-report:prod` preflight command plus an output artifact that contains the live environment checklist output and `Live checklist report written:` marker. This ties the blocker JSON in the release summary back to a captured command run. Fixtures now prove valid blocked evidence includes the blocker-report preflight and reject blocked evidence that only references the summary JSON.
- Hardened referenced evidence path handling so directories cannot masquerade as artifacts. The completed evidence validator now requires the evidence note path, preflight artifacts, section screenshot/log artifacts, release JSON/PNG/device artifacts, and live blocker reports to be regular files before reading them. Invalid directory paths now produce clear validation failures instead of filesystem exceptions. Fixtures now reject directory paths for section artifacts, release artifacts, and blocker reports.
- Tightened release-gate checkbox validation. The completed evidence validator now requires all five release-gate checkbox rows to be present and checked in the Release Gate Summary: live Google provider, live shared-link write/email, deployed public preview, browser checkout, and full device smoke. This prevents a note from passing with filled evidence fields while omitting the visible release-gate checklist. Fixtures now reject evidence summaries that omit the checked release-gate rows.
- Tightened clean-release blocker-report consistency. When `Remaining blockers` is `None`, the completed evidence validator now requires `Live blocker report` to be `None` as well, so a clean release summary cannot also point at blocker-report evidence. Fixtures now reject no-blocker evidence that still references a blocker report.
- Strengthened live blocker report freshness validation. The live environment checklist report now writes `generatedAt`, and completed evidence with remaining blockers must point at a blocker report whose generated date matches the run-context `Date`. This prevents stale blocker reports from being reused when final release evidence is intentionally blocked. Fixtures now prove the report writer emits `generatedAt`, and completed evidence rejects stale or undated blocker reports.
- Strengthened live JSON proof freshness validation. The completed evidence validator now requires the run-context `Date` to use `YYYY-MM-DD`, and Google OAuth, shared-link live, and public-preview JSON evidence artifacts must include `checkedAt` with the same date. This prevents stale live proof artifacts from being reused in a newer hands-on evidence note while leaving screenshots and manual device logs flexible. Fixtures now reject loose date formats, stale `checkedAt` values, and missing `checkedAt` in live JSON artifacts.
- Strengthened screenshot/log artifact validation for completed evidence. Section and device evidence now validate JPEG screenshots with a JPEG signature instead of accepting any non-empty `.jpg`/`.jpeg` file. Final device-smoke evidence also accepts `.txt` and `.log` artifacts, matching the evidence template's screenshot/log wording. Fixtures now prove valid JPEG section evidence passes, invalid JPEG section evidence fails, and a log-based device-smoke artifact can close the device-smoke evidence field.
- Strengthened completed evidence secret scanning across referenced artifacts. The evidence validator now scans text-like artifacts (`.json`, `.log`, `.md`, `.txt`, and `.xml`) linked from preflight output, section screenshots/logs, device smoke evidence, live release JSON evidence, and live blocker reports for bearer tokens, Google ID-token assignments, owner bearer env assignments, live share-token URLs, password assignments, and OTP values. Fixtures now reject secret leakage inside referenced artifacts, not only inside the Markdown evidence note.
- Strengthened final preflight artifact content validation. Preflight evidence artifacts must now contain output markers from the matching command, so a non-empty file cannot be substituted for the hands-on checklist, live env checklist, live readiness, or full readiness output. When `Remaining blockers` is `None`, the live-readiness and full-readiness artifacts must also show their command-specific passed markers. Evidence with remaining blockers can still pass only when the blocker report is valid, allowing failed readiness logs to document the blocked release state without pretending the release gate passed.
- Strengthened final preflight evidence validation. Checked hands-on checklist, live env checklist, live readiness, and full readiness preflight commands must now reference an absolute `.log`, `.txt`, `.md`, or `.json` output artifact, and the validator verifies the referenced file exists and is non-empty. JSON preflight artifacts must parse. The evidence template now prompts for preflight output artifacts, and fixtures cover both passing preflight logs and missing preflight artifact failures.
- Strengthened per-section hands-on evidence validation. Every completed smoke section now requires `Screenshots or logs` to reference an absolute screenshot/log artifact path (`.png`, `.jpg`, `.jpeg`, `.json`, `.xml`, `.md`, `.txt`, or `.log`), and the validator verifies the referenced file exists and is non-empty. PNG artifacts must have a valid PNG signature and JSON artifacts must parse. The evidence template now prompts for absolute section artifact paths, and fixtures generate temporary per-section artifacts plus a negative case for a missing section artifact.
- Strengthened final device-smoke evidence validation. The production evidence template now asks for the hands-on checklist command or full-device-smoke note plus an absolute screenshot/log artifact path, and the completed evidence validator accepts safe device artifacts (`.png`, `.jpg`, `.jpeg`, `.json`, `.xml`, or `.md`) only when the referenced file exists and is non-empty. PNG artifacts must have a valid PNG signature, and JSON artifacts must parse. Fixtures now reject missing device-smoke artifact paths, missing files, and empty device-smoke artifacts.
- Strengthened final release artifact content validation inside completed hands-on evidence. Google OAuth artifacts must now prove `auth.verifyMobileGoogle`, profile/business context, email match, returned bearer session, and tenant/profile alignment. Shared-link live artifacts must prove public page fetch, metadata verification, order request, notification dispatch, follow-up recording, and link deactivation. Public preview artifacts must prove checkout HTML hooks, Open Graph/Twitter metadata, image alt checks, image fetches, and WhatsApp-style preview user agent. Fixtures now reject valid JSON files that do not contain the expected proof markers.
- Strengthened final release artifact validation inside completed hands-on evidence. The validator now opens the referenced Google OAuth, shared-link live, and public-preview JSON artifacts, verifies they exist and parse as JSON, and verifies the browser checkout screenshot exists with a PNG signature. Fixtures now create temporary value-safe artifacts, prove valid artifacts pass, and reject missing or invalid artifact files.
- Strengthened blocker-report validation inside completed hands-on evidence. When blockers remain, the validator now opens the referenced blocker JSON, verifies the file exists, parses as JSON, has report type `mobile-retail-ops-live-env-checklist`, and contains `missingRequired` rows. Fixtures now prove evidence with a valid blocker report can pass, while evidence that references a missing blocker report fails.
- Tightened the completed hands-on evidence validator around live blockers. The release summary now includes a `Live blocker report` field, and if `Remaining blockers` is anything other than `None`, the validator requires that field to reference `qa:mvp-live-blocker-report:prod` plus an absolute `.json` blocker report path. This keeps final evidence from closing with loose blocker prose when the production live gates still need external values.
- Promoted the live blocker JSON to a first-class production QA command. `bun run --cwd apps/mobile qa:mvp-live-blocker-report:prod` now writes `/tmp/ewatrade-mvp-live-blockers.json` from `.env.production`, and `qa:mvp-smoke-flow` guards the command so it stays in the release workflow. The production hands-on evidence template now includes the blocker-report command as a preflight step, while the default local template omits it to avoid confusing local fallback QA with release evidence.
- Added a value-safe live blocker report output for the production live-env checklist. Operators can now set `MVP_LIVE_ENV_CHECKLIST_REPORT_PATH` to an absolute `.json` path while running `qa:mvp-live-env-checklist:prod`; the report contains section names, key names, missing/configured/optional status, and verification commands without configured values. Fixture coverage proves the report writes, rejects unsafe relative/non-JSON paths, and does not leak configured Google tokens, bearer tokens, customer passwords, share URLs, emails, or sample endpoints. A current production report was generated at `/tmp/ewatrade-mvp-live-blockers.json` with 22 missing required live items across 5 sections.
- Refreshed the production release gate after the Brain migration. `bun run --cwd apps/mobile qa:mvp-local-readiness` passed source QA, MVP typechecks, contract tests, Google OAuth readiness, and local readiness fixtures. `Pixel_3a_API_34` was then booted headlessly, `bun run --cwd apps/mobile qa:android-ready` passed with `emulator-5554` online, and `bun run --cwd apps/mobile qa:mvp-readiness:prod` passed source QA, typechecks, contract tests, Android readiness, and base Google OAuth readiness before failing only on live proof inputs. The emulator was shut down cleanly after the run. Remaining release blockers are the fresh Google ID token/profile confirmation, shared-link live write/email owner/product/customer/follow-up values, deployed share URL plus expected metadata values, and browser checkout disposable customer credentials/confirmation.
- Tightened final release evidence validation so the hands-on evidence note must reference the durable artifacts written by the live proof runners. Google OAuth live, shared-link live write/email, and public preview evidence now require absolute `.json` artifact paths, browser checkout requires an absolute `.png` screenshot path, and the evidence template now prompts for command plus artifact path on each release evidence line.
- Tightened release evidence validation so filled release-gate evidence fields must reference the actual proof commands. Google, shared-link live, public preview, and browser checkout evidence now require their matching `qa:*` command markers, and device smoke evidence must reference the hands-on checklist or explicit full device smoke proof. Fixture coverage now rejects vague evidence text even when release checkboxes are checked.
- Recovered Android readiness for the strict production release gate. `Pixel_3a_API_34` was started, ADB attached `emulator-5554`, Android completed boot, and `qa:android-ready` passed. A fresh `qa:mvp-readiness:prod` run then passed source QA, typechecks, MVP contracts, Android readiness, and base Google OAuth readiness before failing only on externally supplied live proof values.
- Tightened final hands-on evidence validation so completed evidence notes must include the hands-on checklist, live env checklist, live readiness, and full readiness preflight commands. The validator accepts local/default or production `:prod` command variants, and fixture coverage now fails evidence notes that omit the live-readiness or full-readiness proof.
- Tightened top-level `qa:mvp-readiness` so failed full-release runs print the required-only missing value summary inline whenever live release gates fail. Operators now see failing gates, live env key names, and recommended next commands in one full readiness run, without configured values or secret markers. Fixture coverage protects inline summary output, production `:prod` command guidance, and secret suppression.
- Tightened `qa:mvp-live-readiness` so failed live-readiness runs print the required-only missing value summary inline. Operators now get the failing gates, required env names, and recommended next commands in one run, without configured values or secret markers. Fixture coverage protects inline summary output, production `:prod` command guidance, env-file propagation to child gates, and secret suppression.
- Added a required-only missing value summary to `qa:mvp-live-env-checklist`. The production checklist now ends with a compact list of the remaining required live inputs by section and env key name, excluding optional unset rows and never printing configured values. Fixture coverage protects the summary, optional exclusion behavior, and secret-scrubbing contract.
- Clarified the live environment checklist so optional blank values are no longer presented as blockers. `qa:mvp-live-env-checklist` now renders optional unset rows as `[optional unset]` while required live inputs remain `[missing]`. Fixture coverage proves configured optional values still render as configured, optional unset values do not show as missing, and secret values are not printed. Production checklist output now leaves the remaining blocker list focused on real live inputs only.
- Filled safe local and production live-QA evidence output paths in `.env` and `.env.production`. Google OAuth live, shared-link live, public preview, and browser checkout now have absolute `/tmp/...` destinations for release evidence JSON/PNG artifacts. Production live readiness no longer reports missing evidence path or browser screenshot path; the remaining failures are the real live proof inputs: explicit confirmations, fresh Google ID token, expected profile email/mode, owner bearer token, synced product/share URL, disposable customer credentials, and follow-up mode.
- Added a reusable Expo EAS env attachment guard after verifying the live Expo setup. `qa:expo-env-attachment` now checks the local Expo project identity, required development/production mobile env keys, and `eas.json` build profile contract; it can also validate captured `eas env:list --format long` evidence or query Expo directly through `qa:expo-env-attachment-live`. Fixtures prove valid attachment evidence passes, missing production attachment fails, wrong project id fails, and static-only mode remains available for source QA. The check is wired into `qa:mvp-source`, its fixture is wired into `qa:mvp-contracts`, `qa:mvp-smoke-flow` guards the verifier markers, and the live verifier passed against the real `@cipron-startups/ewatrade` project.
- Verified Expo environment attachment for the existing `@cipron-startups/ewatrade` project. EAS is authenticated as `ishaqyusuf` with owner access to `cipron-startups`, and project info matches `532f9a55-f4f6-4d4e-b60b-ea6fa8807a3b`. `apps/mobile/.env.local` was pushed to Expo `development` and `apps/mobile/.env.production` was pushed to Expo `production`; Expo returned `No new variables to push` for both, then `env:list` confirmed the expected project-scoped mobile keys are present in development and production. Chrome was opened to the Expo environment variables page and the EwaTrade project page was verified to show development and production envs with all expected mobile variable names. `qa:mobile-env-config` passed.
- Tightened deployed public preview release evidence. `qa:shared-link-preview` now requires `SHARED_LINK_PREVIEW_EVIDENCE_PATH` as an absolute `.json` path and writes a value-safe summary after WhatsApp-style page fetch, URL shape validation, checkout HTML hook validation, canonical/Open Graph/Twitter metadata checks, image alt/business context checks, and preview image fetches. The summary avoids share tokens and full private URLs. Fixtures prove the evidence file is created, non-empty, contains metadata proof, and does not leak the share token/full URL. `qa:mvp-live-readiness:prod` now reports both missing preview URL and missing preview evidence path as expected external live setup blockers.
- Tightened live Google OAuth release evidence. `qa:google-oauth-live-ready` now requires `GOOGLE_LIVE_EVIDENCE_PATH` as an absolute `.json` path, and the Google live runner writes a value-safe JSON summary after `auth.verifyMobileGoogle` proves bearer-session creation, matching profile email, business context, and tenant/profile alignment. The summary avoids Google ID tokens, returned bearer tokens, and full email addresses. The live checklist and `.env.example` now document the required path, fixtures validate missing/invalid paths and scrubbed local mock output, mobile TypeScript passes, and `qa:mvp-live-readiness:prod` now reports the missing Google live evidence JSON path as an expected external live setup blocker.
- Tightened mutating shared-link live write/email release evidence. `qa:shared-link-live-ready` now requires `SHARED_LINK_LIVE_EVIDENCE_PATH` as an absolute `.json` path, and the live runner writes a value-safe JSON summary only after link creation, public page/metadata verification, public order request creation, notification audit, follow-up, and link deactivation succeed. The summary avoids bearer tokens, customer passwords, full private share URLs, and Google tokens. The live checklist and `.env.example` now document the required path, fixtures validate missing/invalid paths, mobile TypeScript passes, and `qa:mvp-live-readiness:prod` now reports the missing shared-link live evidence JSON path as an expected external live setup blocker.
- Tightened deployed browser checkout release evidence. `qa:shared-link-browser-ready` now requires `SHARED_LINK_BROWSER_SCREENSHOT_PATH` as an absolute `.png` path, the Playwright runner creates the parent directory and saves a full-page screenshot after the order-requested state appears, and the browser checkout fixture verifies a non-empty screenshot is written. The live checklist and `.env.example` now call the screenshot path required. `qa:shared-link-browser-fixtures`, `qa:mvp-live-env-checklist-fixtures`, `qa:mvp-live-readiness-fixtures`, `qa:mvp-contracts`, and `qa:mvp-source` passed; `qa:mvp-live-readiness:prod` still fails only on expected external/live values and now reports the missing browser screenshot path.
- Tightened final hands-on evidence validation so checked release gates must include written proof. The evidence template now asks for Google, shared-link live, public preview, browser checkout, and device smoke evidence, while `qa:mvp-hands-on-evidence-file` fails on unchecked preflight commands, blank live-gate evidence fields, unchecked release gates, blank smoke evidence, and obvious token/password/share-token leaks. `qa:mvp-hands-on-evidence-file-fixtures`, `qa:mvp-hands-on-evidence-template-fixtures`, `qa:mvp-smoke-flow`, `qa:mvp-contracts`, and `qa:mvp-source` passed.
- Added a clear public unavailable page for invalid, missing, expired, or deactivated shared product links. The shared-product route keeps rejecting unsafe link lookups with `notFound()`, while the nearest storefront `not-found.tsx` now renders `Product link unavailable` copy that tells customers to ask the business for a new product link. `qa:shared-product-storefront`, `bun --filter @ewatrade/storefront typecheck`, and `qa:mvp-source` passed.
- Added public shared-link checkout coverage for deactivated links while narrowing the website-link release gate. Durable inactive share links now have a DB test proving `getRetailOpsSharedProduct` rejects before the public page is shown, and metadata fallback inactive links now have a DB test proving public order creation rejects before customer/order/reservation writes. `qa:shared-product-storefront` guards the source markers that keep inactive durable and fallback links unavailable before customers can order.
- Hardened generated product share-link host selection while checking the remaining website-link release concerns. The DB share-link URL builder now skips unsafe stored hostnames, including non-HTTP protocols, localhost, IPs, and local-device hosts, and continues to the next safe business hostname or generated tenant subdomain instead of falling back too early. Added DB tests for unsafe custom-host skipping and generated tenant-subdomain fallback, and updated `qa:product-share-management` so the business-host safety markers stay guarded.
- Added a validator for the completed hands-on evidence note so the MVP QA ticket cannot be closed with an empty template. `qa:mvp-hands-on-evidence-file` now requires a completed evidence file path, verifies the required sections and run-context fields, rejects unchecked smoke/release checkboxes, requires filled Evidence/Screenshots/Notes entries for every smoke section, requires an explicit Remaining blockers value, and blocks obvious OTP, bearer-token, password, Google-token, and live share-token leakage. The evidence template now points operators to the validator after the note is complete and leaves `QA mode` blank instead of offering inline placeholder options. The validator has fixtures and is wired into `qa:mvp-contracts`, while `qa:mvp-smoke-flow` now guards the script surface.
- Tightened the aggregate readiness runners so remaining release blockers now produce an explicit next-command path instead of only listing failing gates. `qa:mvp-live-readiness` now recommends the focused checklist, Google live ready/run, shared-link live ready/run, deployed preview, and browser checkout commands with the correct `:prod` suffix when running against `.env.production`. `qa:mvp-readiness` now does the same for source QA, typechecks, contracts, Android readiness, Google OAuth, shared-link, and local readiness recovery commands. Added `qa:mvp-readiness-fixtures` and wired it into `qa:mvp-contracts` so the full release runner's command guidance is fixture-protected. A production dry run of `qa:mvp-live-readiness:prod` now passes base Google OAuth readiness and fails only on externally supplied live proof values while printing the exact production next commands.
- Captured fresh Expo Go dashboard launch evidence on `Pixel_3a_API_34`. Metro switched to Expo Go mode, opened `exp://192.168.18.4:3002` on `emulator-5554`, and completed the Android bundle for `expo-router/entry.js`. The app foreground was `host.exp.exponent/host.exp.exponent.experience.ExperienceActivity`; the dashboard rendered with `Ewatrade QA Store`, pending sync conflict copy, inventory actions including Share, Rep sessions, Clock in, and Subscription controls. Light evidence is saved at `/tmp/ewatrade-expo-go-launch.png`, dark-mode evidence after `cmd uimode night yes` is saved at `/tmp/ewatrade-expo-go-dark-dashboard.png`, and the Android UI hierarchy is saved at `/tmp/ewatrade-expo-go-window.xml`. This is current launch/dashboard proof only; the full hands-on smoke run and external live gates still remain open.
- Recovered Android readiness proof on `Pixel_3a_API_34` and tightened release env propagation while rerunning the full production gate. The emulator booted headlessly, `qa:android-ready` passed with `emulator-5554` online, and `qa:mvp-readiness:prod` now passes source QA, typechecks, MVP contracts, Android readiness, and base Google OAuth readiness before failing only on the expected external live proof gates. During the run, two fixture leaks were fixed: Google live readiness now runs its nested base OAuth check against the selected live env file, and the aggregate readiness runners now pass their selected env file to child gates unconditionally so inherited parent env values cannot mask fixture failures. The emulator was shut down cleanly after verification.
- Added `bun run --cwd apps/mobile qa:mvp-hands-on-evidence-template` and `qa:mvp-hands-on-evidence-template:prod` so the final manual smoke run has a value-safe evidence note skeleton instead of an ad hoc note. The template covers safe evidence rules, preflight commands, run context, every MVP smoke section, and the release-gate summary for live Google, live shared-link write/email, deployed preview, browser checkout, and full device smoke. The hands-on checklist now tells testers to start from the matching evidence template, and contract/smoke-flow guards verify local/default and production outputs without leaking tokens, share URLs, customer passwords, or bearer values.
- Made the live environment checklist section instructions production-aware. When the checklist is pointed at `.env.production`, the Google OAuth, Google live API, shared-link live write/email, shared-link preview, and browser checkout sections now tell operators to run the matching `:prod` commands instead of the generic local/default commands. Fixture coverage now checks both default `.env` wording and `.env.production` wording without leaking configured values. `qa:mvp-live-env-checklist:prod` confirms the production checklist body points to the production commands and still reports only missing live proof values.
- Added `bun run --cwd apps/mobile qa:mvp-hands-on-checklist:prod` so the final manual smoke pass can be printed in production mode without accidentally pointing operators at local live gates. The checklist now declares whether it targets the default local env or production env file, includes the live-readiness and full-readiness preflights, and swaps to `qa:mvp-live-env-checklist:prod`, `qa:mvp-live-readiness:prod`, and `qa:mvp-readiness:prod` when production mode is selected. Fixture coverage now checks both local/default and production outputs without printing secret values, and `qa:mvp-smoke-flow` guards the production checklist script.
- Added production-env variants for the individual live QA commands so final live proof can be run piece by piece against `.env.production`: Google base/live ready/run, shared-link live ready/run, shared-link preview, and browser ready/checkout. The live checklist now prints those `:prod` commands, the smoke-flow guard protects them, and the shared-link live runner accepts `SHARED_LINK_LIVE_ENV_FILE` before passing the same file into its readiness preflight.
- Added production-specific live gate commands so the remaining release setup can be checked against `.env.production` without swapping local files. `qa:mvp-live-env-checklist:prod`, `qa:mvp-live-readiness:prod`, and `qa:mvp-readiness:prod` select the production env file; the live-readiness and full-readiness runners propagate that file to Google OAuth, shared-link write/email, deployed preview, and browser checkout child gates. `qa:mvp-live-readiness:prod` now reads production API/storefront/email/OAuth base values and fails only on missing live proof inputs: fresh Google ID token, explicit confirmations, owner bearer token, synced product id, deployed share URL, and disposable customer/browser credentials.
- Added `bun run --cwd apps/mobile qa:mobile-env-config` and wired it into `qa:mvp-source` so the completed root/mobile local and production env split cannot drift silently. The guard checks local API/web/platform/email values, production API/storefront/platform/email values, mobile Expo development/production variant and URL values, required `.env.example` documentation keys, and stale copied GND/Versadesk env markers. `bun run --cwd apps/mobile qa:mobile-env-config`, `bun run --cwd apps/mobile qa:mvp-source`, and `bun run --cwd apps/mobile qa:mvp-local-readiness` passed.
- Added `bun run --cwd apps/mobile qa:mvp-local-readiness` as the green implementation-local readiness gate. It runs source QA, MVP workspace typechecks, MVP contract tests, and base Google OAuth readiness while intentionally skipping Android device readiness, live Google API proof, deployed preview, shared-link live write/email, and browser checkout gates. The fixture is included in `qa:mvp-contracts`, the hands-on smoke checklist now uses local readiness as its first preflight, and strict `qa:mvp-readiness` points to the local command when release-only gates fail. The strict readiness run now passes local gates and fails on release-only gates, including an unauthorized physical Android device `R5CX153J81W` that needs USB debugging approval before hands-on QA.
- Cleaned up `.brain/workflows/retail-ops-stock-to-closeout-flow.md` so the operating workflow no longer describes completed first-phase bridges as future work. The workflow now reflects durable opening-stock movement and price-history rows, movement rows for stock adjustment/unit conversion when available, one-time hashed staff invite tokens, invite notification dispatch, public invite-token resolution, OTP staff onboarding, opening inventory declarations, idempotent sale replay, customer-book persistence, Better Auth shared-link checkout, stock reservations, notification audit, protected follow-up payment/fulfillment, and delivery bridges. Targeted DB query and mobile sync/store tests passed.
- Re-audited the remaining release gates after completing Google/Expo environment setup. `bun run --cwd apps/mobile qa:mvp-typechecks` passes across mobile, API, storefront, database, utilities, notifications, email, and jobs. `bun run --cwd apps/mobile qa:mvp-live-readiness` now passes the base Google OAuth readiness section and fails only on externally supplied live proof values: Google live API token/target, shared-link live write/email setup, deployed public preview URL, and browser checkout disposable customer credentials.
- Completed the Google/Expo OAuth environment setup that previously blocked the base Google readiness gate. The new `ewatrade-mobile-retail-ops` Google Cloud project now has web, Android dev, Android production, iOS dev, and iOS production OAuth clients; the Expo `@cipron-startups/ewatrade` project has matching development, production, and preview env vars; and the root/mobile local and production env files contain the matching Google client IDs. `bun run --cwd apps/mobile qa:google-oauth-ready` passes against root local, root production, mobile local, and mobile production env files.
- Aligned the mutating live shared-link runner with the richer WhatsApp/social preview contract. `qa:shared-link-live-run` now uses a WhatsApp-style preview user agent for the generated public page fetch and validates Open Graph image alt, Open Graph site name, and Twitter image alt before live order creation. `qa:mvp-smoke-flow` now guards those runner markers. `bun run --cwd apps/mobile qa:mvp-smoke-flow`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `bun run --cwd apps/mobile qa:mvp-source`, `bun run --cwd apps/mobile qa:mvp-contracts`, and `bun run --cwd apps/mobile qa:shared-link-live-fixtures` passed.
- Added Twitter/X card image-alt parity for shared product links. The storefront now emits a structured Twitter image descriptor with `alt`, and `qa:shared-link-preview` requires `twitter:image:alt` to include the expected product and business names. `bun run --cwd apps/mobile qa:shared-link-preview-fixtures` and `bun run --cwd apps/mobile qa:shared-product-storefront` passed.
- Made business/store context explicit in shared-link rich metadata. The storefront now emits `og:site_name` from `sharedProduct.store.name`, and `qa:shared-link-preview` requires that site name to match the expected business name in addition to checking image alt text. `bun run --cwd apps/mobile qa:shared-link-preview-fixtures` and `bun run --cwd apps/mobile qa:shared-product-storefront` passed.
- Strengthened WhatsApp/social preview source QA for shared product links. `qa:shared-link-preview` now requires `og:image:alt`, verifies the expected product appears in the image alt text, and fixture-proves page fetches use the WhatsApp-style preview user agent. `qa:shared-product-storefront` also protects the storefront metadata alt marker. `bun run --cwd apps/mobile qa:shared-link-preview-fixtures` and `bun run --cwd apps/mobile qa:shared-product-storefront` passed.
- Aligned the safe live setup checklist with the existing live-local rehearsal flags. `qa:mvp-live-env-checklist` now surfaces `GOOGLE_LIVE_ALLOW_LOCALHOST` and `SHARED_LINK_LIVE_ALLOW_LOCALHOST` beside the live Google and shared-link write/email gates, while the fixture verifies both labels without leaking configured values. `bun run --cwd apps/mobile qa:mvp-live-env-checklist-fixtures`, `bun run --cwd apps/mobile qa:mvp-live-readiness-fixtures`, and `bun run --cwd apps/mobile qa:mvp-live-env-checklist` passed.
- Hardened the shared-link browser checkout readiness gate so the reused deployed share URL key is documented and guarded. `qa:shared-link-browser-ready` now requires `.env.example` to include `SHARED_LINK_PREVIEW_URL` alongside the browser checkout controls, and `qa:shared-link-browser-fixtures` covers the missing-documentation failure. `bun run --cwd apps/mobile qa:shared-link-browser-fixtures`, `bun run --cwd apps/mobile qa:mvp-contracts`, and `bun run --cwd apps/mobile qa:mvp-live-env-checklist` passed.
- Hardened the public preview gate so setup documentation cannot drift. `qa:shared-link-preview` now checks `.env.example` for the deployed URL, expected host, expected product/business names, and local-fixture allowance keys before fetching the public page. `qa:shared-link-preview-fixtures` now covers the missing-documentation failure alongside valid metadata, product-image metadata, localhost opt-in, expected product mismatch, and missing share-token cases. `bun run --cwd apps/mobile qa:shared-link-preview-fixtures` passed.
- Completed another value-free live checklist alignment pass for the public preview gate. `qa:shared-link-preview` already supports `SHARED_LINK_PREVIEW_ALLOW_LOCALHOST` for local fixtures, and `qa:mvp-live-env-checklist` now surfaces that optional flag in the Shared-Link Public Preview section so local fixture allowances are visible before release and not confused with production QA. `bun run --cwd apps/mobile qa:mvp-live-env-checklist-fixtures` and `bun run --cwd apps/mobile qa:mvp-live-env-checklist` passed.
- Tightened shared-link live write/email QA setup around order quantity. The guarded live runner already supports `SHARED_LINK_LIVE_QUANTITY`, and the readiness gate now requires `.env.example` to keep documenting it, validates any configured value as a positive whole number, and the value-free live checklist shows the optional quantity control alongside the product, customer, notification, and follow-up inputs. `bun run --cwd apps/mobile qa:shared-link-live-fixtures`, `bun run --cwd apps/mobile qa:mvp-live-env-checklist-fixtures`, and `bun run --cwd apps/mobile qa:mvp-live-env-checklist` passed.
- Added `bun run --cwd apps/mobile qa:mvp-hands-on-checklist` as a repeatable, value-free checklist for the final hands-on MVP smoke pass. It prints preflight commands, evidence expectations, and step-by-step device checks for launch/auth, first product/dashboard, staff/session, sale/customer book, offline/sync, inventory/reports/subscription, and share-link web checkout. `qa:mvp-smoke-flow` now protects the checklist scripts, and `qa:mvp-contracts` runs the checklist fixture so the remaining manual QA path cannot drift into an untracked note. `bun run --cwd apps/mobile qa:mvp-hands-on-checklist`, `bun run --cwd apps/mobile qa:mvp-hands-on-checklist-fixtures`, and `bun run --cwd apps/mobile qa:mvp-smoke-flow` passed.
- The live setup checklist now includes the full shared-link browser checkout configuration surface: deployed share URL, explicit browser order confirmation, register/login mode, disposable customer identity, optional quantity, phone, notes, screenshot path, headful mode, and localhost allowance. The browser checkout readiness gate also requires `.env.example` to document those runner controls, while the checklist continues to print only configured/missing status instead of secret values. `bun run --cwd apps/mobile qa:shared-link-browser-fixtures`, `bun run --cwd apps/mobile qa:mvp-live-env-checklist-fixtures`, `bun run --cwd apps/mobile qa:mvp-live-readiness-fixtures`, `bun run --cwd apps/mobile qa:mvp-live-env-checklist`, and `bun run --cwd apps/mobile qa:mvp-source` passed. `bun run --cwd apps/mobile qa:shared-link-browser-ready` still correctly fails until a deployed product link and disposable browser checkout credentials are configured.
- The shared floating bottom-sheet primitive is now aligned with the GND mobile reference. The shared `Modal` wrapper uses a `BottomSheetBackdrop` with close-on-press behavior, a rounded theme-aware background component, platform bottom inset, side inset, elevated detached container, bounded max dynamic content height from the active window size, pan-down close, Android `adjustResize`, keyboard restore, and interactive keyboard behavior. `qa:keyboard-coverage` and `qa:mvp-surfaces` now guard those markers while still blocking raw `BottomSheetModal` JSX and `detached={false}` in feature sheets. `bun run --cwd apps/mobile qa:keyboard-coverage`, `bun run --cwd apps/mobile qa:nativewind-style`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, and `bun run --cwd apps/mobile qa:mvp-source` passed.
- API architecture QA now covers the focused Subscription router split. Subscription snapshot reads and provider-neutral checkout intent creation live in `retail-ops-subscriptions.ts` and are merged into the existing `retailOps.*` namespace, reducing the core router while preserving mobile dashboard and subscription sheet procedure names. `bun run --cwd apps/mobile qa:subscription-flow`, `bun run --cwd apps/mobile qa:retail-ops-api-boundary`, `bun run --cwd apps/mobile qa:mvp-source`, `bun --filter @ewatrade/api typecheck`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, and `bun run --cwd apps/mobile qa:mvp-contracts` passed.
- API architecture QA now covers the focused Staff router split. Staff invite, staff listing/status updates, staff stock wallets, stock assignment/return, public invite-token resolution, and authenticated staff onboarding procedures live in `retail-ops-staff.ts` and are merged into the existing `retailOps.*` namespace, reducing the core router while preserving mobile staff invite, staff onboarding, dashboard, and offline sync procedure names. `bun run --cwd apps/mobile qa:staff-flow`, `bun run --cwd apps/mobile qa:retail-ops-api-boundary`, `bun run --cwd apps/mobile qa:mvp-source`, `bun --filter @ewatrade/api typecheck`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, and `bun run --cwd apps/mobile qa:mvp-contracts` passed.
- API architecture QA now covers the focused Stock router split. Stock movement reads, stock intake, manual stock adjustment, and unit conversion procedures live in `retail-ops-stock.ts` and are merged into the existing `retailOps.*` namespace, reducing the core router while preserving mobile stock intake, unit conversion, reports, and offline sync procedure names. `bun run --cwd apps/mobile qa:inventory-operations-flow`, `bun run --cwd apps/mobile qa:reports-flow`, `bun run --cwd apps/mobile qa:retail-ops-api-boundary`, `bun run --cwd apps/mobile qa:mvp-source`, `bun --filter @ewatrade/api typecheck`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, and `bun run --cwd apps/mobile qa:mvp-contracts` passed.
- API architecture QA now covers the focused Sessions router split. Session listing, payment reconciliation, open session, close session, and closeout review procedures live in `retail-ops-sessions.ts` and are merged into the existing `retailOps.*` namespace, reducing the core router while preserving mobile rep clock-in, closeout, reports, and dashboard procedure names. `bun run --cwd apps/mobile qa:inventory-operations-flow`, `bun run --cwd apps/mobile qa:reports-flow`, `bun run --cwd apps/mobile qa:retail-ops-api-boundary`, `bun run --cwd apps/mobile qa:mvp-source`, `bun --filter @ewatrade/api typecheck`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, and `bun run --cwd apps/mobile qa:mvp-contracts` passed.
- API architecture QA now covers the focused Sales router split. Sale creation, recent sales, credit sales, and credit payment procedures live in `retail-ops-sales.ts` and are merged into the existing `retailOps.*` namespace, reducing the core router while preserving mobile create-sale, dashboard, and report procedure names. `bun run --cwd apps/mobile qa:create-sale-flow`, `bun run --cwd apps/mobile qa:dashboard-production-flow`, `bun run --cwd apps/mobile qa:reports-flow`, `bun run --cwd apps/mobile qa:retail-ops-api-boundary`, `bun run --cwd apps/mobile qa:mvp-source`, `bun --filter @ewatrade/api typecheck`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, and `bun run --cwd apps/mobile qa:mvp-contracts` passed.
- API architecture QA now covers the focused Product router split. Product setup, unit templates, effective price reads, price history, and price updates live in `retail-ops-products.ts` and are merged into the existing `retailOps.*` namespace, reducing the core router while preserving mobile tRPC procedure names. `bun run --cwd apps/mobile qa:first-product-flow`, `bun run --cwd apps/mobile qa:retail-ops-api-boundary`, `bun run --cwd apps/mobile qa:mvp-source`, `bun --filter @ewatrade/api typecheck`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, and `bun run --cwd apps/mobile qa:mvp-contracts` passed.
- API architecture QA now covers the first focused Retail Ops router split. Product share-link management, generated-link analytics, shared-link order follow-up, and delivery follow-up procedures live in `retail-ops-share-links.ts` and are merged into the existing `retailOps.*` namespace, reducing the core router while preserving mobile tRPC procedure names. `bun run --cwd apps/mobile qa:product-share-management`, `bun run --cwd apps/mobile qa:retail-ops-api-boundary`, `bun run --cwd apps/mobile qa:mvp-source`, `bun --filter @ewatrade/api typecheck`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, and `bun run --cwd apps/mobile qa:mvp-contracts` passed.
- Product links QA now covers detailed generated-link analytics in the mobile sheet. The sheet reads `retailOps.productShareLinkAnalytics`, refreshes it with production link/order mutations, and shows unique visitors, requests, completed/cancelled requests, reservation movement quantities, revenue, and top links while keeping the compact floating sheet surface. `bun run --cwd apps/mobile qa:product-share-management`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `bun run --cwd apps/mobile qa:mvp-source`, and `bun run --cwd apps/mobile qa:mvp-contracts` passed.
- Stock adjustment QA now blocks production-invalid reason/direction combinations before they reach tRPC or the offline replay queue. Damage/loss adjustments resolve to decrease, found-stock adjustments resolve to increase, correction can still increase or decrease, and the stock sheet explains disabled direction choices inline. `bun test apps/mobile/src/store/retailOpsStore.test.ts`, `bun run --cwd apps/mobile qa:inventory-operations-flow`, `bun run --cwd apps/mobile qa:mvp-source`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, and `bun run --cwd apps/mobile qa:mvp-contracts` passed.
- The shared floating bottom-sheet contract is now actively guarded. All mobile feature `*-sheet.tsx` files must route through the shared `Modal` primitive, raw `BottomSheetModal` JSX is blocked outside the primitive, and feature sheets cannot opt out with `detached={false}`. The shared modal keeps detached radius, side inset, bottom inset, theme-derived shadow/background, Android resize, keyboard restore, and interactive keyboard defaults. `bun run --cwd apps/mobile qa:mvp-surfaces`, `bun run --cwd apps/mobile qa:keyboard-coverage`, `bun run --cwd apps/mobile qa:mvp-source`, and `bun --cwd apps/mobile tsc --noEmit --pretty false` passed.
- First-product setup now includes an optional product description field that feeds production product creation, local fallback product state, and queued offline `product_setup` replay. This gives shared product pages and WhatsApp/social metadata a real product-description source from the first owner setup flow. `bun test apps/api/src/schemas/retail-ops.test.ts apps/mobile/src/lib/retail-ops-sync.test.ts apps/mobile/src/store/retailOpsStore.test.ts`, `bun run --cwd apps/mobile qa:first-product-flow`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `bun --filter @ewatrade/api typecheck`, `bun --filter @ewatrade/db typecheck`, `bun run --cwd apps/mobile qa:mvp-source`, and `bun run --cwd apps/mobile qa:mvp-contracts` passed. A hands-on keyboard/device pass for the new optional description input and deployed social scrape remain pending.
- Product share-link handoff now has an explicit copy-to-clipboard path in addition to the native share sheet. Local fallback and production generated-link rows show `Copy link`, call Expo Clipboard, and briefly switch to `Link copied` on the copied row. `qa:product-share-management` now guards the clipboard markers, and `bun run --cwd apps/mobile qa:product-share-management`, `bun run --cwd apps/mobile qa:nativewind-style`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `bun run --cwd apps/mobile qa:mvp-source`, and `bun run --cwd apps/mobile qa:mvp-contracts` passed. A hands-on device tap of the new copy button still needs a future device QA pass.
- Optional product image links now enter through first-product setup and flow through production product creation, local fallback state, and queued offline `product_setup` sync. The API accepts optional HTTP(S) `imageUrl`, product creation stores it in `Product.metadata.retailOps.imageUrl`, and the shared-link preview path can use that image as the real product preview source. `bun test apps/api/src/schemas/retail-ops.test.ts packages/db/src/queries/retail-ops-products.test.ts apps/mobile/src/lib/retail-ops-sync.test.ts`, `bun run --cwd apps/mobile qa:first-product-flow`, `bun --filter @ewatrade/api typecheck`, `bun --filter @ewatrade/db typecheck`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `bun run --cwd apps/mobile qa:mvp-source`, and `bun run --cwd apps/mobile qa:mvp-contracts` passed. This is still URL-based and does not add product image upload/storage.
- Shared-link WhatsApp/social metadata now supports real product images when the product carries a public image URL in structured metadata. Shared product lookup returns `product.imageUrl`, the storefront prefers that public image for Open Graph/Twitter previews, and the generated 1200x630 `/api/og/shared-product` card remains the fallback for missing, relative, local, or unsafe image URLs. The public-preview validator now accepts either product/CDN images or the generated card, with fixture coverage for both. `bun test packages/db/src/queries/retail-ops-share-links.test.ts`, `bun test apps/storefront/src/app/p/[tenantSlug]/[storeSlug]/[productSlug]/shared-product-order-utils.test.ts`, `bun run --cwd apps/mobile qa:shared-product-storefront`, `bun run --cwd apps/mobile qa:shared-link-preview-fixtures`, `bun --filter @ewatrade/db typecheck`, `bun --filter @ewatrade/storefront typecheck`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, and `bun run --cwd apps/mobile qa:mvp-contracts` passed. A first-class product image upload/storage flow and live WhatsApp scraper proof still remain outside this slice.
- Google OAuth live API readiness now composes the base Google OAuth readiness proof. The standalone `qa:google-oauth-live-ready` gate checks the live API URL, explicit auth confirmation, fresh ID token, expected email, and mode, then also requires the base client ID/audience/debug-keystore readiness covered by `qa:google-oauth-ready`. Fixture coverage proves the happy path with synthetic Android/iOS/web client IDs and a temporary debug keystore, plus a failing API-audience mismatch case. `bun run --cwd apps/mobile qa:google-oauth-live-fixtures`, `bun run --cwd apps/mobile qa:mvp-live-readiness-fixtures`, and `bun run --cwd apps/mobile qa:mvp-contracts` passed; the real live-ready command still correctly fails until live Google/API env values are configured.
- Static QA confirmed the auth screens use the shared keyboard-aware `MobileScreen` wrapper, and modal form surfaces use the bottom-sheet keyboard-aware wrapper where expected.
- Fixed the only scanned `className` plus `style` mix in `apps/mobile/src` by moving dynamic safe-area/background styles into style-only wrapper nodes in `MobileScreen`.
- Targeted Biome and whitespace checks passed for the mobile auth screens, shared screen primitive, first-product setup, create-sale, staff invite, dashboard, and subscription sheet.
- Android Expo Go QA on `Pixel_3a_API_34` exposed a Reanimated invalid shadow color crash from `shadow-black/5`; removed that shadow token from shared mobile primitives and reloaded successfully.
- Android visual QA confirmed the login placeholder now uses prompt copy (`Enter your email address`) and the login keyboard state keeps the email field and submit action usable above the keyboard.
- Replaced sample-value placeholders across the MVP mobile auth, business, staff, first-product, sale, closeout, stock intake, unit conversion, and share-link delivery forms with prompt-style copy such as `Enter your email address`.
- Targeted placeholder scan passed for the confusing sample values (`owner@business.com`, `aisha@example.com`, `Amina`, `Aisha`, `Starter`, `Half bag`, numeric expected values, and delivery examples) in mobile placeholders.
- Android Expo Go QA confirmed the login screen's full sign-up card navigates to the sign-up route after simplifying auth CTA navigation to the shared haptic `Pressable` `href` path directly, avoiding nested `Link asChild` behavior on the custom pressable.
- Android Expo Go QA confirmed sign-up email focus remains above the keyboard on a compact phone, and the login email flow keeps the submit action visible while the keyboard is open.
- Android Expo Go QA exposed the OTP `path.split` crash on Android `TextInput`; removed OTP autofill props and moved the OTP cells to style-only React Native styling so the OTP route renders cleanly.
- Android Expo Go QA confirmed the OTP cells open the numeric keyboard and keep the verify action visible above the keyboard.
- Verification pass cleared the current mobile typecheck blockers and confirmed `bun --cwd apps/mobile tsc --noEmit --pretty false`, `bun --filter @ewatrade/db typecheck`, `bun --filter @ewatrade/api typecheck`, and `bun --filter @ewatrade/utils typecheck` all pass after the React 19/shared UI, first-product reconciliation, reports credit aging, sync icon, toast, Zod helper, and closeout session query fixes.
- Android Expo export passed through the workspace env wrapper and local Expo CLI, bundling `expo-router/entry.js` for Android with 8,794 modules and producing the Hermes bundle plus metadata; the generated export artifact was removed after verification.
- Android Expo Go QA on `Pixel_3a_API_34` confirmed the sign-up screen renders with prompt-style placeholders and the email-focused keyboard state remains usable on a compact phone.
- Android Expo Go QA confirmed the local fallback OTP route renders cleanly after restart and can complete into the dashboard/first-product setup path.
- Android Expo Go QA exposed and fixed a dashboard redbox from unstable Zustand selector snapshots in sheet/dashboard components; filtered business-scoped arrays now derive from raw store arrays with `useMemo`.
- Android Expo Go QA exposed and fixed first-product bottom-sheet keyboard handling for lower numeric fields. Bottom-sheet form inputs now use Gorhom-aware text input behavior with style-only theme styling, shared sheet keyboard spacer defaults, and modal Android keyboard defaults (`adjustResize`, `restore`, `interactive`). Final screenshot evidence shows the price field visible above the numeric keyboard.
- Android Expo Go QA confirmed local fallback sessions now add the first product through the local queue instead of failing protected production tRPC with `You must be signed in to continue`; dashboard showed Rice queued with 10 Bags and 1 local change pending sync.
- Android Expo Go QA confirmed clock-in queues locally from the Rep sessions sheet, opens the sales day, and moves the dashboard to 2 local changes pending sync.
- Android Expo Go QA exposed and fixed the Create Sale Android `TextInput`/NativeWind crash by removing `text-center` from the custom quantity input path and applying text alignment through style-only input props.
- Android Expo Go QA exposed and fixed the Create Sale quantity row overflow; minus and plus controls now remain visible beside the bounded input, and selecting Rice Bag plus stepping quantity to 2 updates total to `NGN 37,000.00`.
- Android Expo Go QA exposed and fixed Create Sale customer-entry keyboard coverage by moving the sheet to the shared bottom-sheet keyboard-aware scroll view, forwarding the bottom-sheet scroll ref, and nudging checkout focus so the active customer field remains visible above the keyboard.
- Android Expo Go QA completed a local cash sale for 2 Bags; dashboard returned with Rice stock reduced from 10 Bags to 8 Bags and 4 local changes pending sync.
- Android Expo Go QA then corrected the Create Sale keyboard workaround back to a virtualized `BottomSheetSectionList`. Product rows and checkout controls are now all virtualized rows, and focusing the customer row uses `scrollToLocation` so the customer input remains visible above the keyboard without losing the long-list contract.
- Android Expo Go QA confirmed the staff invite sheet opens from the real dashboard `Add staff` quick action, uses prompt-style placeholders, keeps the email field visible above the Android keyboard, and can scroll to the submit action after keyboard dismissal.
- Android Expo Go QA exposed and fixed a staff invite fallback mismatch: when production staff is unavailable and the sheet shows `Local fallback`, submit now queues through the local staff store instead of calling the protected production mutation and showing `You must be signed in to continue.` Retest confirmed a valid invite closes the sheet and updates dashboard staff usage from `0/2` to `1/2`.
- Android Expo Go QA confirmed the owner dashboard `Plans` action opens the subscription sheet, local fallback billing state is visible, current Starter trial usage is readable, Growth and Pro tiers are reachable with `Online required`, and `Done` is reachable at the bottom. The smoke exposed and fixed raw fallback error copy by removing the passive production subscription query error from the local fallback sheet; retest confirmed the friendly fallback and production-billing notices remain without `You must be signed in to continue.`
- Android Expo Go QA confirmed the product `Share` action opens the Product links sheet from the real Rice product card, local generated-link metrics and local-link mode copy render without overlap, generated-link management is reachable, and the `Pending link orders` plus `Delivery follow-up` sections show calm production-unavailable states with `Refresh` and `Done` still reachable. The current local fallback session did not contain authenticated production order rows, so completing/cancelling real shared-link orders and delivery status mutation remain unverified.
- Android Expo Go light/dark QA exposed and fixed a shared text contrast regression. In Android dark mode, the dashboard background switched dark but most shared `Text` content stayed black; the shared `Text` primitive now resolves semantic text color classes against the active theme. Retest confirmed the dark dashboard and Create Sale sheet render readable headings, muted copy, banners, product cards, quantity controls, totals, and payment controls, and a patched light-mode Create Sale capture remained readable.
- Follow-up NativeWind audit tightened the shared `Text` fallback so it only passes a `style` prop when a caller supplied style or when the active-theme color fallback is required, avoiding unnecessary style injection on text nodes that can remain className-only. `bunx biome check --write apps/mobile/src/components/ui/text.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, and `git diff --check` passed.
- Source-level long-list audit confirmed Create Sale already uses `BottomSheetSectionList` for product/unit rows, converted Customer book customer rows to `BottomSheetFlatList`, and bounded Product links product selection to a searchable 12-row visible window instead of rendering every product in the share-management scroll body. `bunx biome check --write apps/mobile/src/components/mobile/customer-book-sheet.tsx apps/mobile/src/components/mobile/product-share-sheet.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and the targeted map/list source scan passed.
- Offline/sync source hardening aligned the dashboard banner, Sync status sheet, and Reports sync summary around the MVP promise that offline changes will be applied when next the user connects. The dashboard banner now top-aligns wrapping content and keeps icons from squeezing the message; the Sync status sheet gives the offline toggle copy a flexible column and shows a compact current-device label with the full device id constrained to one line. `bunx biome check --write apps/mobile/src/app/dashboard.tsx apps/mobile/src/components/mobile/sync-status-sheet.tsx apps/mobile/src/components/mobile/reports-sheet.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and the targeted offline-copy/layout source scan passed.
- Attempted the next Android visual QA pass for offline banner/sync surfaces with Metro on port 3002 and `Pixel_3a_API_34`, but the emulator process twice failed to attach to ADB: `adb devices -l` stayed empty and `adb connect localhost:5555` returned `Connection refused` after an ADB server restart. The failed emulator and Metro sessions were cleaned up. Added stable QA selectors for the same surfaces: `retail-sync-banner`, `retail-offline-toggle`, `retail-offline-device`, `retail-sync-pending-count`, `retail-sync-retry-count`, `retail-sync-review-count`, `retail-sync-conflicts-reviewed`, `retail-sync-failed-retry`, `retail-sync-now`, and `retail-sync-queue`. `bunx biome check --write apps/mobile/src/app/dashboard.tsx apps/mobile/src/components/mobile/sync-status-sheet.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and the selector scan passed.
- Source-level dark-mode audit hardened shared input primitives. The legacy controlled `Input` now uses semantic classes plus `useColors()` instead of fixed light-mode label, placeholder, border, background, text, and icon colors; the modern `input-2` primitive now supplies the active theme's muted placeholder color for normal and bottom-sheet inputs; and `FormField` no longer passes a fixed placeholder color. `bunx biome check --write apps/mobile/src/components/ui/Input.tsx apps/mobile/src/components/ui/input-2.tsx apps/mobile/src/components/mobile/form-field.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and the targeted fixed-color scan passed.
- Source-level contrast audit hardened the shared toast primitive. Warning toasts no longer use white text on amber; the style-only toast palette now uses dark slate text on amber and darker backgrounds for success, error, and info variants. Calculated warning-toast contrast improved from `2.15:1` to `8.31:1`. `bunx biome check --write apps/mobile/src/components/ui/toast/toast.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, the targeted toast color marker scan, and the contrast calculation passed.
- Source-level shared-control audit hardened the `ProgressBar`. The component no longer uses a fixed hex fill palette, derives default fill colors from the active theme, and now respects `fillClassName` instead of always taking the inline-color branch. `bunx biome check --write apps/mobile/src/components/ui/progress-bar.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and the targeted progress-bar marker scan passed.
- Source-level action primitive audit hardened remaining raw press actions. Modal close now uses the shared haptic `Pressable` with transition while the animated backdrop keeps its raw pressable for Reanimated compatibility, and toast actions now use `Pressable` plus light haptic feedback instead of `TouchableOpacity`. `bunx biome check --write apps/mobile/src/components/ui/modal.tsx apps/mobile/src/components/ui/toast/toast.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and the targeted `TouchableOpacity`/haptic marker scans passed.
- Source-level bottom-sheet visibility audit hardened the shared modal handle. Default non-detached handles no longer use `bg-background` against the same active background as the sheet; both header and hidden-header paths now use `bg-muted-foreground/25`, matching the detached handle affordance. `bunx biome check --write apps/mobile/src/components/ui/modal.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and the targeted modal handle scan passed.
- Source-level long-list audit hardened stock-related product pickers. Stock intake and unit conversion now keep full product lists searchable while rendering bounded 12-product visible windows, empty search states, and match-count copy instead of rendering every product option in a bottom-sheet scroll body. Selection still resolves against the full list. `bunx biome check --write apps/mobile/src/components/mobile/stock-intake-sheet.tsx apps/mobile/src/components/mobile/unit-conversion-sheet.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and the targeted visible-window source scan passed.
- Source-level long-list audit hardened Product links management sections. Local generated links and production links now render bounded 8-row visible windows, while pending link orders and delivery follow-up rows render bounded 6-row visible windows; truncated sections show match-count copy. `bunx biome check --write apps/mobile/src/components/mobile/product-share-sheet.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and the targeted visible-array source scan passed.
- Source-level long-list audit hardened inventory declaration forms. Rep clock-in opening stock and closeout closing stock now render searchable bounded 12-line visible windows with empty search states and match-count copy, while validation and submit payloads still use the full inventory line set. `bunx biome check --write apps/mobile/src/components/mobile/rep-clock-in-sheet.tsx apps/mobile/src/components/mobile/closeout-sheet.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and the targeted visible-line source scan passed.
- Source-level long-list audit hardened Reports rows. The shared `ReportSection` now renders a bounded eight-row visible window and shows count copy when rows are truncated, covering source, conflict review, sync operations, sales, credit, stock, movement, and variance sections without changing the full row calculations used for metrics. `bunx biome check --write apps/mobile/src/components/mobile/reports-sheet.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and the targeted `visibleRows` source scan passed.
- Source-level NativeWind style-mix audit scanned all mobile TSX native JSX openings for same-element `className` plus `style` usage on `View`, `Text`, `TextInput`, `Pressable`, animated pressables, `KeyboardAvoidingView`, and `ScrollView`. The focused `bun -e` scan found zero native same-element openings with both props, so no code patch was needed.
- Converted the NativeWind style-mix audit into a repeatable mobile QA script. `bun run --cwd apps/mobile qa:nativewind-style` now scans mobile TSX native JSX openings and fails when `className` and `style` appear on the same native element, preserving the user's NativeWind rule as an executable guard.
- Source-level dashboard long-list hardening bounded the first-product variant preview. The dashboard inventory-started card now renders the first four variants and shows count copy when more variants exist, while Share and Create sale remain the full-list paths. `bunx biome check --write apps/mobile/src/app/dashboard.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and the targeted `DASHBOARD_VARIANT_PREVIEW_LIMIT`/`visibleVariants` source scan passed.
- Source-level sync sheet long-list hardening bounded remaining offline/sync rows. Device management, server sync history, server conflicts, blocked dependency rows, and the local sync queue now render through named visible arrays with count copy when truncated. `bunx biome check --write apps/mobile/src/components/mobile/sync-status-sheet.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and targeted visible-array source scans passed.
- Source-level dark-mode contrast hardening updated the shared `Text` primitive. Default text now uses semantic `text-foreground`, secondary text uses `text-muted-foreground`, and fixed amber/emerald/green/red status tokens lift to active-theme warning/success/error colors in dark mode. Calculated contrast improved from `3.56:1` amber and `3.26:1` emerald to `8.31:1` warning and `5.42:1` success against the dark app background. `bunx biome check --write apps/mobile/src/components/ui/text.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, targeted resolver scans, and contrast calculation passed.
- Source-level long-list hardening bounded the Unit Conversion target variant picker. The sheet now keeps selected-product variants searchable while rendering an eight-variant visible window, empty search state, and count copy when matches are truncated, instead of rendering every variant at once. `bunx biome check --write apps/mobile/src/components/mobile/unit-conversion-sheet.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and targeted variant visible-window source scans passed.
- Source-level long-list hardening bounded business and staff previews. Business switching now searches business name/type/currency/country/category and renders an eight-business visible window with empty search state and count copy, while Staff invite renders named `visibleStaffRows` with count copy beyond six attendants. `bunx biome check --write apps/mobile/src/components/mobile/business-switch-sheet.tsx apps/mobile/src/components/mobile/staff-invite-sheet.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and targeted source scans passed.
- Source-level long-list hardening bounded Stock Intake unit and movement previews. The selected product's unit picker now has a searchable eight-unit visible window, empty search state, and count copy, while recent stock movements render through a named eight-row visible window with count copy. `bunx biome check --write apps/mobile/src/components/mobile/stock-intake-sheet.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and targeted unit/movement visible-window scans passed.
- Source-level dashboard long-list hardening added named preview limits and count copy for rep sessions, low-stock alerts, attendants, product links, stock movements, customer previews, and recent sales. Anonymous dashboard `slice(0, 3)` preview renders were replaced with named visible arrays. `bunx biome check --write apps/mobile/src/app/dashboard.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and targeted dashboard preview scans passed.
- Source-level share-link follow-up hardening added per-operation guards to the Product links sheet. Production link deactivation, shared-link order completion/cancellation, delivery request creation, and delivery status updates now track the active link/order/delivery id, prevent accidental second submissions while the first mutation is settling, clear the operation on settlement, and show row-level in-flight labels such as `Deactivating`, `Completing`, `Cancelling`, `Creating request`, and `Updating`. `bunx biome check --write apps/mobile/src/components/mobile/product-share-sheet.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and the targeted NativeWind style-mix source scan passed.
- Source-level first-product flow hardening split the initial inventory setup into a lighter two-step sheet. The owner now enters item details and optional variants first, then continues to a dedicated current-stock step, matching the requested onboarding sequence and reducing first-screen form bulk. `bunx biome check --write apps/mobile/src/components/mobile/first-product-setup-sheet.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and the targeted step/stock source scan passed.
- Source-level copied-data cleanup removed the remaining poultry-feed fallback catalog/order copy from mobile static product data. Legacy cart/category fallback files now use retail inventory wording (`Rice`, `Beans`, `Garri`, `Palm Oil`, `Cooking Gas`, and neutral category names) and no longer contain the copied sample terms caught by the spec scan. `bunx biome check --write apps/mobile/src/data/products-data.tsx apps/mobile/src/data/orders-data.ts apps/mobile/src/data/categories-data.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and targeted copied-catalog scans passed.
- Source-level role QA hardened attendant dashboards. Cashier/operator profiles no longer auto-open first-product setup, no longer route empty-inventory New sale into owner setup, show neutral assigned-inventory copy when no product is available, keep product Share available, and hide Restock, Convert, and stock-movement Record actions. `bunx biome check --write apps/mobile/src/app/dashboard.tsx`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and targeted role-guard source scans passed.
- Source-level placeholder/copy guard reran the app-facing placeholder scan after the request to avoid sample placeholder values. Mobile and shared-product storefront placeholders remain direct prompts rather than sample names, emails, businesses, addresses, products, or numbers, and local fallback auth/session code no longer injects fake sample-looking email addresses (`owner@ewatrade.test` or `local@ewatrade.test`) into local profile state. `bunx biome check --write apps/mobile/src/hooks/use-auth.tsx apps/mobile/src/lib/session-store.ts`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, `git diff --check`, and targeted sample email/name scans passed.
- Converted the prompt-placeholder audit into a repeatable mobile QA script. `bun run --cwd apps/mobile qa:prompt-placeholders` now scans mobile source plus the shared-product storefront route for `placeholder` props containing sample emails, people names, sample product/unit names, example wording, or starter/sample values, while allowing those words in tests and non-placeholder fixtures where they are not user-facing placeholder prompts.
- Added a repeatable source-structure guard for the core MVP surfaces. `bun run --cwd apps/mobile qa:mvp-surfaces` now verifies the auth routes keep `MobileScreen`, OTP keeps `OtpInput`, sheet-heavy workflows keep `BottomSheetKeyboardAwareScrollView`, Create Sale keeps `BottomSheetSectionList`, Customer Book keeps `BottomSheetFlatList`, and list-heavy surfaces keep bounded preview markers.
- Folded the GND-style floating bottom sheet behavior into the shared `Modal` primitive so all MVP sheets inherit detached/floating presentation, platform bottom inset, side inset, rounded clipped surface, elevated container, active theme card/border colors, and Android resize/restore/interactive keyboard defaults. `qa:keyboard-coverage` and `qa:mvp-surfaces` now protect those floating-sheet markers.
- Bundled the repeatable source guards into one command. `bun run --cwd apps/mobile qa:mvp-source` now runs the MVP surface-structure, NativeWind style-mix, and prompt-placeholder checks in one source QA pass before the remaining hands-on device QA.
- Android emulator retry still could not resume hands-on QA. `Pixel_3a_API_34` exists and launched, but `adb wait-for-device` never returned, `adb devices -l` stayed empty, and the emulator opened only transient localhost ports instead of the normal ADB transport. The wait and emulator sessions were stopped cleanly with no qemu process left running.
- Added `bun run --cwd apps/mobile qa:android-ready` as a repeatable Android hands-on QA readiness gate. The command confirms ADB, the Android emulator binary, and the expected `Pixel_3a_API_34` AVD are present, then fails until an online Android device or emulator is visible to ADB. The current run still reports no online ADB device attached.
- Retried launching `Pixel_3a_API_34` directly with the Android emulator CLI. The emulator passed initial compatibility checks and opened localhost ports `62810` and `62811`, but ADB did not attach the emulator as an online device. Explicit `adb connect` attempts to both ports failed, and `adb devices -l` showed those transports as `offline`. The emulator session and offline transports were cleaned up, returning ADB to an empty device list; a narrow `lsof -i -P -a -c qemu-syst` check returned no leftover qemu network process.
- Added `bun run --cwd apps/mobile qa:mvp-readiness` as the top-level readiness gate. It runs source QA, focused MVP workspace typechecks, MVP contract tests, Android readiness, and Google OAuth readiness in one pass; the current run passes source QA, typechecks, and contract tests, then reports the remaining live blockers as Android ADB attachment plus missing Google OAuth client IDs/audiences.
- Hardened the Android readiness gate to parse ADB transport states before deciding readiness. The command now reports offline localhost emulator transports, unauthorized physical devices, and unknown ADB states explicitly before the generic online-device requirement. A deterministic fixture run reproduced the recent `localhost:62810` and `localhost:62811` offline transport failure so the diagnostic path is covered even while the current local ADB list is empty.
- Added `bun run --cwd apps/mobile qa:theme-colors` and included it in `qa:mvp-source`. The guard scans app routes, screens, mobile feature components, and shared app-facing controls for fixed hex colors, fixed black/white NativeWind tokens, fixed JSX color props, and string-literal inline colors. The legacy not-found route and logout control were converted to semantic theme tokens, `useColors()`, and the shared haptic `Pressable` before the guard was enabled.
- Added `bun run --cwd apps/mobile qa:auth-onboarding` and included it in `qa:mvp-source`. The guard verifies the mobile launch/auth routes keep splash handling, keyboard provider wiring, Google sign-in, email OTP login/sign-up, a no-password lightweight owner signup form, OTP auto-submit/resend/local fallback behavior, platform Google ID-token handling, and invite-based minimal staff onboarding.
- Added `bun run --cwd apps/mobile qa:create-sale-flow` and included it in `qa:mvp-source`. The guard verifies Create Sale keeps the virtualized product/unit picker, display-only product headers, variant and primary-unit selection rules, stock disabling, haptic plus/minus numeric quantity entry, same-screen total preview, cash/transfer payment capture, customer entry/customer-book lookup, production `retailOps.createSale`, and local queued fallback.
- Added `bun run --cwd apps/mobile qa:first-product-flow` and included it in `qa:mvp-source`. The guard verifies the dashboard empty-inventory prompt, two-step first-product setup, keyboard-safe sheet, unit templates plus local suggestions, optional variants with manual prices and conversion multipliers, current-stock entry, production `retailOps.createProduct`, remote-id reconciliation, local fallback, opening-stock movement creation, and queued `product_setup` sync event behavior.
- Added `bun run --cwd apps/mobile qa:offline-sync-flow` and included it in `qa:mvp-source`. The guard verifies the local sync envelope keeps device identity, dependency metadata, retry/conflict states, idempotency keys, blocked-event reasons, production-id reconciliation, the dashboard offline/sync banner, the Sync status sheet's device registration/replay/retry/conflict-review wiring, stable sync QA selectors, and Reports conflict visibility/filtering.
- Added `bun run --cwd apps/mobile qa:staff-flow` and included it in `qa:mvp-source`. The guard verifies the Staff invite sheet keeps production staff listing/invites, local queued fallback, subscription staff limits, bounded staff rows, email invite copy, cashier role submission, invite-token onboarding, compact attendant setup, invited-staff dashboard redirect, role-gated owner actions, protected API permissions, hidden acceptance tokens, and shared email/notification invite payloads.
- Added `bun run --cwd apps/mobile qa:subscription-flow` and included it in `qa:mvp-source`. The guard verifies the three-tier Starter/Growth/Pro model, business-scoped limits, default Starter trial, persisted local fallback state, dashboard plan entry point, production subscription snapshot, mobile plan/usage surface, provider-neutral checkout intent, safe checkout URL opening, and billing permission/API boundaries.
- Added `bun run --cwd apps/mobile qa:product-share-management` and included it in `qa:mvp-source`. The guard verifies the Product links sheet keeps keyboard-safe share-link management, production link/order/delivery procedures, local share-link fallback, native share handoff, bounded previews, pending link order follow-up, delivery follow-up, payment/fulfillment fields, row-level pending-operation guards, durable share-link queries, fulfillment fallback queries, and offline sync events.
- Added `bun run --cwd apps/mobile qa:customer-book-flow` and included it in `qa:mvp-source`. The guard verifies checkout customer entry and selection, production customer search, local fallback options, selected customer identity forwarding, the virtualized Customer book sheet, dashboard previews, local customer save/update, queued `customer_upsert` sync events, production id reconciliation, tenant-scoped API reads, durable/order-derived customer queries, and shared-link customer capture.
- Added `bun run --cwd apps/mobile qa:dashboard-production-flow` and included it in `qa:mvp-source`. The guard verifies authenticated production dashboard reads, local fallback metrics, recent-sale dedupe, staff/customer preview merges, attendant role gating, invited-staff redirect, production-aware first-product prompting, offline/sync banner visibility, reusable sheet entry points, production tenant id seeding, and mobile session role/status handoff.
- Added `bun run --cwd apps/mobile qa:inventory-operations-flow` and included it in `qa:mvp-source`. The guard verifies production/local clock-in, closeout, stock intake, stock adjustment, and unit conversion paths; synced-id gating; cash/transfer and inventory declarations; local queue events with dependencies and device metadata; typed tRPC procedures; whole-number schemas; and durable stock/session query entry points.
- Added `bun run --cwd apps/mobile qa:reports-flow` and included it in `qa:mvp-source`. The guard verifies the Reports sheet keeps production and local fallback report source handling, sales by attendant/product, cash/transfer totals, stock balances, stock movements, closeout/payment variance, credit aging, tenant and current-device sync conflict visibility, bounded report rows, protected report APIs, and DB report query entry points.
- Added `bun run --cwd apps/mobile qa:mvp-typechecks` and included it in `qa:mvp-readiness`. The typecheck gate covers the mobile app plus the API, storefront, DB, utils, notifications, email, and jobs packages that support the MVP flows.
- Added `bun run --cwd apps/mobile qa:mvp-contracts` and included it in `qa:mvp-readiness`. The contract gate runs the focused API Google auth verifier tests, API retail-ops schema tests, mobile quantity/offline-sync/subscription helper tests, and shared-link notification dispatch tests. Current readiness now proves source QA, focused workspace typechecks, and 28 MVP contract tests pass before stopping on the remaining live Android/OAuth setup gates.
- Added focused DB mobile-auth query tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 35 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on the remaining live Android/OAuth setup gates.
- Added focused DB shared-link follow-up query tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 37 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on the remaining live Android/OAuth setup gates.
- Added focused DB retail-ops fulfillment query tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 39 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on the remaining live Android/OAuth setup gates.
- Added focused DB shared-product order request creation tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 41 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on the remaining live Android/OAuth setup gates.
- Added focused DB product share-link lifecycle tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 43 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on the remaining live Android/OAuth setup gates.
- Added the Google OAuth readiness fixture command to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 43 focused Bun tests, and the value-free OAuth fixture command before stopping on the remaining live Android/OAuth setup gates.
- Added `bun run --cwd apps/mobile qa:android-ready-fixtures` and folded it into `qa:mvp-contracts`. The fixture command proves Android readiness diagnostics for an online emulator, offline localhost transports, unauthorized devices, and a missing required AVD without requiring a live emulator. Current readiness now proves source QA, focused workspace typechecks, 43 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on the remaining live Android/OAuth setup gates.
- Added focused DB retail-ops staff invite/onboarding query tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 46 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added focused DB retail-ops customer book/upsert query tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 49 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added focused DB retail-ops product setup query tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 50 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added focused DB retail-ops sales query tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 52 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added focused DB retail-ops session open/closeout query tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 55 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added focused DB retail-ops stock operation query tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 59 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added focused DB retail-ops sync ledger query tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 62 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added focused DB retail-ops subscription query tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 65 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added focused DB retail-ops dashboard/report query tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 68 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Extended focused DB retail-ops sales query tests to cover credit sales listing, sales-by-rep payment buckets, and credit payment settlement. Current readiness now proves source QA, focused workspace typechecks, 71 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Extended focused DB retail-ops session query tests to cover closed-session listing and payment reconciliation report rows with declaration metadata, opening/closing inventory declarations, review metadata, credit totals, expected cash, and per-method variance. Current readiness now proves source QA, focused workspace typechecks, 73 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added focused DB retail-ops staff stock wallet tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 76 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Extended focused DB shared-link query tests to cover generated-link listing, durable/metadata de-duplication, rollup counters, and filtered daily analytics. Current readiness now proves source QA, focused workspace typechecks, 78 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Extended focused DB fulfillment query tests to cover delivery request listing with durable/metadata merge, actor-scoped durable filters, same-order de-duplication, metadata-only fallback rows, and requested-time ordering. Current readiness now proves source QA, focused workspace typechecks, 79 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Extended focused DB retail-ops product query tests to cover unit-template merging and unit price-history merging. Current readiness now proves source QA, focused workspace typechecks, 81 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Extended API schema and source-guard coverage for product unit effective-price reads, price-history filters, unit price updates, and the product pricing/template tRPC API boundary. Current readiness now proves source QA, focused workspace typechecks, 83 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added focused API mobile-auth schema tests to cover lightweight OTP signup, OTP verification, Google identity verification, trimmed email normalization, strict no-password payloads, invalid OTP rejection, unsupported mode rejection, and too-short Google token rejection. Current readiness now proves source QA, focused workspace typechecks, 87 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Extended API schema coverage for Retail Ops contact emails across checkout sale payloads, customer upserts, staff invites, and shared-link order requests. Current readiness now proves source QA, focused workspace typechecks, 89 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Extended API schema and storefront coverage for optional phone fields across sale checkout, customer upsert, shared-link checkout, delivery creation, and delivery status updates. Current readiness now proves source QA, focused workspace typechecks, 91 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added focused API tenant/store setup schema tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 95 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added focused DB tenant store creation tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 97 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added focused storefront shared-product order helper tests to `qa:mvp-contracts`. Current readiness now proves source QA, focused workspace typechecks, 103 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added `bun run --cwd apps/mobile qa:app-launch` and included it at the start of `qa:mvp-source`. The guard verifies the Expo launch names, schemes, bundle/package ids, automatic light/dark style, splash plugin wiring, light/dark splash images, contain resize mode, splash width, and required launcher/splash PNG dimensions; it also caught and fixed the development display-name typo from `Ewrtrade Dev` to `Ewatrade Dev`.
- Added Reports CSV export to close the MVP reporting export gap. The Reports sheet now exports visible report rows, report source state, active business, and the current sync-device conflict filter through the native share sheet, backed by a pure CSV helper and focused Bun tests. Current readiness now proves source QA, focused workspace typechecks, 105 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Expanded the theme-color guard to scan shared `components/ui` primitives in addition to app routes, screens, and mobile feature components. This exposed and fixed fixed-color shared-control gaps: destructive button text now uses `text-destructive-foreground`, toast status palettes and shadow color derive from the active theme, alert-dialog and bottom-sheet overlays use semantic foreground overlays, and the guard no longer treats the Text variant parser's `color: "default"` metadata as an inline color. Current readiness now proves expanded source QA, focused workspace typechecks, 105 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added `bun run --cwd apps/mobile qa:long-lists` and included it in `qa:mvp-source`. The guard centralizes the long-list QA checklist by verifying virtualized product/customer rows and bounded preview windows with count copy across Create Sale, Customer Book, Product Links, Stock Intake, Unit Conversion, Rep Clock-in, Closeout, Reports, Sync Status, Dashboard, Staff Invite, and Business Switch. Current readiness now proves source QA with the long-list guard, focused workspace typechecks, 105 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added `bun run --cwd apps/mobile qa:keyboard-coverage` and included it in `qa:mvp-source`. The guard centralizes the keyboard QA checklist by verifying the app keyboard provider, shared `MobileScreen`, bottom-sheet input provider, modal Android keyboard defaults, route-level auth/OTP/staff onboarding wrappers, OTP and quantity numeric inputs, Create Sale checkout/customer focus support, Customer Book input context, and keyboard-aware first-product, staff invite, clock-in, closeout, stock intake, and unit conversion sheets. Current readiness now proves source QA with the keyboard guard, focused workspace typechecks, 105 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Added `bun run --cwd apps/mobile qa:offline-visuals` and included it in `qa:mvp-source`. The guard centralizes source coverage for offline banner placement, sync status states, and conflict messaging by verifying the dashboard wrapping sync banner, Sync status mode/device/history/conflict/retry/review surfaces, stable QA selectors, bounded rows, business-impact and resolution copy, and Reports tenant/current-device conflict review plus CSV export. Current readiness now proves source QA with the offline visual guard, focused workspace typechecks, 105 focused Bun tests, Android readiness fixtures, and Google OAuth readiness fixtures before stopping on Android device readiness and Google OAuth readiness because no online Android device/emulator is attached and local `.env` lacks real Google OAuth client IDs/audiences.
- Tightened the shared-product storefront source guard inside `qa:mvp-source`. The guard now protects customer register/login error handling, password validation/autocomplete, platform customer identity forwarding, server-side quantity normalization, and notification failure audit markers in addition to the existing metadata, public lookup, order request, and interactive total checks.
- Tightened the shared-product storefront source guard to cover the OG image endpoint used by rich link previews. The guard now protects the `ImageResponse` route, bounded query parsing, product/business/price rendering, call-to-action copy, and 1200x630 preview dimensions.
- Added `bun run --cwd apps/mobile qa:retail-ops-api-boundary` and included it in `qa:mvp-source`. The guard verifies the Retail Ops tRPC router stays wired through API schemas and DB query modules, keeps public/authenticated/protected procedure boundaries for MVP workflows, exposes the router through the app router, keeps schema validation at the API boundary, and avoids direct `ctx.db.*` Prisma access in the router.
- Added `bun run --cwd apps/mobile qa:action-primitives` and included it in `qa:mvp-source`. The guard verifies the shared `Pressable`, `Button`, and mobile `ActionButton` keep haptic/ripple/transition/navigation behavior, blocks raw React Native touchables in app source, and allows raw Pressable only inside the shared primitive and the Reanimated-compatible modal backdrop. Toast actions were moved onto the shared Pressable haptic path.
- Added `bun run --cwd apps/mobile qa:mobile-auth-api-boundary` and included it in `qa:mvp-source`. The guard protects the owner OTP/Google auth tRPC boundary, mobile auth DB query module, Google verifier, app-router exposure, and no-direct-Prisma-access rule for auth.
- Added `bun run --cwd apps/mobile qa:session-offline-persistence` and included it in `qa:mvp-source`. The guard verifies mobile sessions stay in `expo-secure-store`, authenticated mobile tRPC calls forward the app bearer token, the API context accepts `x-app-authorization`, offline Retail Ops state persists through the shared Zustand storage adapter, and queued sync events keep device/dependency/retry/conflict/idempotency metadata with bounded replay and blocked-event explanations.
- Added `bun run --cwd apps/mobile qa:mvp-smoke-flow` and included it in `qa:mvp-source`. The guard verifies the source QA bundle and package scripts keep every named Mobile Retail Ops MVP smoke area covered by a focused guard: launch/splash, haptic actions, auth/signup/OTP/staff onboarding, first product, dashboard, staff invite, create sale, customer book, inventory operations, offline sync, offline visuals, session/offline persistence, product share management, shared product storefront ordering, reports/export, subscription, keyboard coverage, long-list coverage, NativeWind style discipline, prompt placeholders, theme colors, surface structure, and API boundaries.
- Restored the live Android readiness gate. After resetting ADB and launching `Pixel_3a_API_34` headlessly, `emulator-5554` attached as an online device and `bun run --cwd apps/mobile qa:android-ready` passed. The top-level `bun run --cwd apps/mobile qa:mvp-readiness` now passes source QA, typechecks, contract tests, Android fixtures, Google OAuth fixtures, and live Android device readiness before stopping only on missing Google OAuth client IDs/audiences.
- Cross-package typed-contract verification passed after the recent MVP source hardening: `bun --filter @ewatrade/api typecheck`, `bun --filter @ewatrade/db typecheck`, `bun --filter @ewatrade/utils typecheck`, and `bun --cwd apps/mobile tsc --noEmit --pretty false` all passed. Focused Biome checks passed for the changed API, DB, utils, and recent mobile source files after targeted formatting. Broad `bun --filter @ewatrade/db lint` and a broad mobile Biome pass still report existing generated/package-wide or unrelated shared UI lint debt, so they remain out of scope for closing this MVP QA ticket.
- Android Expo Go QA continued on `Pixel_3a_API_34` with dashboard, Sync status, Product links, Subscription, and Create Sale surfaces. The pass exposed and fixed Create Sale quantity keyboard clipping: bottom-sheet forms now use the Gorhom-aware shared input path, the selected unit renders as a compact one-row summary, the duplicate selected-product header is hidden, and quantity focus/press-in plus unit selection scroll through the virtualized checkout list. Retest evidence shows the quantity input and total remain visible above the Android numeric keyboard.
- Full MVP readiness was rerun after the Android Create Sale keyboard fix. Source QA, focused workspace typechecks, 105 focused Bun tests, Android readiness fixtures, Google OAuth readiness fixtures, and live Android device readiness all pass; the readiness gate now fails only on missing real Google OAuth client IDs/audiences.
- Google auth initiation was hardened after reviewing the local After Service OAuth reference. `useMobileGoogleAuth` now catches provider/session launch failures from `promptAsync()` and shows email-code fallback copy instead of leaving the failed Google launch silent; `bun run --cwd apps/mobile qa:auth-onboarding`, `bun run --cwd apps/mobile qa:mobile-auth-api-boundary`, `bun run --cwd apps/mobile qa:mvp-source`, `bun --cwd apps/mobile tsc --noEmit --pretty false`, and `bun run --cwd apps/mobile qa:google-oauth-fixtures` passed.
- Strengthened long-list QA from marker-only source checks into deterministic stress coverage. `bun run --cwd apps/mobile qa:long-lists` now forbids direct `.map()` rendering for the Create Sale and Customer Book virtualized paths, parses named preview limits from source, and exercises synthetic large-list totals for product/customer suggestions, product-share products/links/orders/deliveries, stock intake, unit conversion, opening/closing stock declarations, reports, sync status rows, dashboard previews, staff rows, and business switching. `bun run --cwd apps/mobile qa:long-lists`, `bun run --cwd apps/mobile qa:mvp-source`, and `git diff --check` passed.
- Added a local Retail Ops MVP smoke contract that walks the offline store through first product setup, opening stock, staff invite, rep clock-in, share-link generation, cash sale with customer capture, stock deduction, closeout, subscription plan state, sync summary, synced-state resolution, and returning online. The test is now included in `qa:mvp-contracts`, and `qa:mvp-smoke-flow` fails if that local smoke contract is removed from the contract bundle. `bunx biome check --write apps/mobile/src/store/retailOpsStore.test.ts apps/mobile/scripts/run-mvp-contract-tests.mjs apps/mobile/scripts/check-mvp-smoke-flow-coverage.mjs`, `bun test apps/mobile/src/store/retailOpsStore.test.ts`, `bun run --cwd apps/mobile qa:mvp-smoke-flow`, `bun run --cwd apps/mobile qa:mvp-contracts`, and `git diff --check` passed.
- Added authenticated API router contract coverage for shared-link order follow-up. `apps/api/src/trpc/routers/retail-ops-follow-up.test.ts` now proves the protected tRPC path completes a shared-link order request through tenant/store/user scope, rejects roles without Retail Ops POS access, creates delivery requests, and updates delivery status with assignment/tracking metadata. `qa:mvp-contracts` includes the test, and `qa:product-share-management` protects the contract wiring. `bunx biome check --write apps/api/src/trpc/routers/retail-ops-follow-up.test.ts apps/mobile/scripts/run-mvp-contract-tests.mjs apps/mobile/scripts/check-product-share-management-flow.mjs`, `bun test apps/api/src/trpc/routers/retail-ops-follow-up.test.ts`, `bun --filter @ewatrade/api typecheck`, `bun run --cwd apps/mobile qa:mvp-contracts`, `bun run --cwd apps/mobile qa:mvp-source`, and `git diff --check` passed.
- Restored live Android readiness and captured another Android Expo Go visual QA pass on `Pixel_3a_API_34`. Metro loaded the app through Expo Go from `exp://10.0.2.2:3002`, the dashboard rendered in light mode with the pending-sync banner, inventory card actions, rep session card, and subscription entry readable (`/tmp/ewatrade-android-current.png`), and Android dark mode rendered the same dashboard state with readable banner/card/action contrast (`/tmp/ewatrade-dark-dashboard.png`). Opening the Sync status sheet in dark mode showed readable online status, offline mode option, offline device identity, server-history error copy, and pending/retry/review counters without text overlap (`/tmp/ewatrade-dark-sync-sheet.png`). Toggling offline mode updated the sheet copy to `Changes stay on this device and will be applied when next you connect`, with the offline dashboard banner visible behind the sheet (`/tmp/ewatrade-dark-offline-sheet.png`). `bun run --cwd apps/mobile qa:android-ready` passed, and full `bun run --cwd apps/mobile qa:mvp-readiness` passed source QA, typechecks, contract tests, Android fixtures, Google OAuth fixtures, and Android device readiness before failing only on missing Google OAuth client IDs/audiences.
- Larger-phone Android dark/offline QA found the dashboard offline banner clipping its long message on a 1440x3120 viewport. The dashboard sync banner now uses a constrained wrapping text wrapper and removes the trailing chevron in offline mode, with fresh evidence at `/tmp/ewatrade-large-dark-offline-fixed-2.png` showing the offline message wrapping cleanly. `bun run --cwd apps/mobile qa:offline-visuals`, `bun run --cwd apps/mobile qa:nativewind-style`, `bun run --cwd apps/mobile qa:dashboard-production-flow`, and `bun --cwd apps/mobile tsc --noEmit --pretty false` passed after the fix.
- Adopted the user-provided dark-mode UI sample as the target direction for mobile dark mode. The spec now calls out a matte black canvas, charcoal cards/chips, crisp white primary text, muted gray metadata, and restrained orange/green accents, while `THEME.dark` and NativeWind CSS variables now use that sample-inspired token set globally. A fresh Android dark-mode screenshot at `/tmp/ewatrade-dark-sample-theme.png` confirmed the dashboard renders with the updated black canvas, orange actions, green status text, muted gray supporting copy, and the larger-phone offline banner still wrapping cleanly. `bun run --cwd apps/mobile qa:theme-colors`, `bun run --cwd apps/mobile qa:nativewind-style`, `bun run --cwd apps/mobile qa:offline-visuals`, `bun run --cwd apps/mobile qa:dashboard-production-flow`, and `bun --cwd apps/mobile tsc --noEmit --pretty false` passed after the token update. The CSS file was not run through Biome formatting because its existing Tailwind `@config` plus `@import` order is currently flagged by Biome.
- Added an executable guard for the user-provided dark-mode sample direction. `qa:theme-colors` now checks the dark theme sections for the matte black, charcoal, white, orange, green, and muted gray tokens and rejects the retired slate/navy dark tokens; because `qa:theme-colors` is part of `qa:mvp-source`, the sample-inspired palette is now covered by the full source QA bundle. `bunx biome check --write apps/mobile/scripts/check-theme-color-guard.mjs`, `bun run --cwd apps/mobile qa:theme-colors`, `bun run --cwd apps/mobile qa:nativewind-style`, `bun run --cwd apps/mobile qa:mvp-source`, and `git diff --check` passed.
- Closed the keyboard-usability checklist item with current evidence. Earlier Android Expo Go passes verified sign-up email focus, OTP numeric cells, first-product lower numeric fields, Create Sale quantity controls, customer entry, checkout completion, and Staff invite email entry remain usable with the keyboard open; the repeatable `qa:keyboard-coverage` guard now protects the shared route/sheet wrappers, bottom-sheet keyboard provider, modal Android resize defaults, OTP/quantity numeric keyboards, and focused scroll behavior for these surfaces. `bun run --cwd apps/mobile qa:keyboard-coverage`, `bun run --cwd apps/mobile qa:mvp-source`, and `git diff --check` passed before marking the item complete.
- Strengthened the offline/conflict visual QA contract for the remaining visual pass. The Sync status sheet now exposes stable selectors for conflict visual review: `retail-sync-conflict-summary`, `retail-sync-event-row`, `retail-sync-event-conflict-detail`, and `retail-sync-event-failed-detail`. `qa:offline-visuals` now protects those selectors plus the conflict status label, fallback conflict message, impact/action copy, reviewed action, and conflict summary copy. `bunx biome check --write apps/mobile/src/components/mobile/sync-status-sheet.tsx apps/mobile/scripts/check-offline-visual-coverage.mjs`, `bun run --cwd apps/mobile qa:offline-visuals`, `bun run --cwd apps/mobile qa:mvp-source`, and `bun --cwd apps/mobile tsc --noEmit --pretty false` passed. The offline banner/sync/conflict visual checklist item remains open until a real conflict-state visual pass is captured on compact and larger Android viewports.
- Completed the real Android dark-mode conflict visual pass with a seeded local sync queue on `Pixel_3a_API_34`. Compact screenshots at `/tmp/ewatrade-conflict-dashboard.png`, `/tmp/ewatrade-conflict-sync-sheet.png`, `/tmp/ewatrade-conflict-sync-summary.png`, and `/tmp/ewatrade-conflict-sync-events.png` confirmed the dashboard conflict banner, floating Sync status sheet, conflict summary, failed-event card, and individual conflict row stay readable without clipping. Larger viewport screenshots at `/tmp/ewatrade-large-conflict-dashboard.png`, `/tmp/ewatrade-large-conflict-sync-sheet.png`, and `/tmp/ewatrade-large-conflict-sync-summary.png` confirmed the same surfaces on a 1440x3120 override.
- Completed the remaining real Android light/dark visual pass on `Pixel_3a_API_34` through Expo Go. Light screenshots at `/tmp/ewatrade-light-dashboard-visual.png`, `/tmp/ewatrade-light-login-visual.png`, `/tmp/ewatrade-light-signup-visual.png`, `/tmp/ewatrade-light-otp-visual.png`, and `/tmp/ewatrade-light-product-share-sheet-visual.png` confirmed the dashboard, conflict banner, auth screens, OTP cells, product-link analytics cards, search input, selected product row, and floating Product links sheet are readable without clipping. Dark screenshots at `/tmp/ewatrade-dark-dashboard-visual.png`, `/tmp/ewatrade-dark-login-visual.png`, `/tmp/ewatrade-dark-signup-visual.png`, `/tmp/ewatrade-dark-otp-visual.png`, `/tmp/ewatrade-dark-subscription-sheet-visual.png`, and `/tmp/ewatrade-dark-product-share-sheet-visual.png` confirmed the sample-inspired dark dashboard, auth screens, OTP cells, subscription sheet, and product share sheet keep readable contrast, orange/green accents, detached floating sheet shape, and non-overlapping controls.
- Added `bun run --cwd apps/mobile qa:shared-link-live-ready` and included its fixture check in `qa:mvp-contracts`. The top-level readiness gate now proves source QA, typechecks, contracts, Android readiness fixtures, Google OAuth fixtures, and shared-link live readiness fixtures before reporting live blockers. `bun run --cwd apps/mobile qa:shared-link-live-run` is now the guarded live execution command for protected link creation, public page fetch, canonical/Open Graph/Twitter metadata validation, OG image fetch validation, public shared-product order mutation, protected notification audit polling, follow-up, and link deactivation once live env values are configured. The current run fails only on Android device readiness, Google OAuth readiness, and shared-link live readiness.
- Added stable shared-product checkout selectors for a future browser click-through pass and protected them in `qa:shared-product-storefront`, including form, unit selection, quantity, total, register/login, customer fields, notes, submit, requested-success, and error states.
- Added `bun run --cwd apps/mobile qa:shared-link-preview` plus fixture coverage in `qa:mvp-contracts` for a non-mutating public share-preview check. The command validates an existing deployed product share URL with a WhatsApp-style user agent, confirms canonical/Open Graph/Twitter metadata, preserves the expected business host, checks expected product/business metadata when provided, and fetches the generated preview image response.
- Wired the deployed public share-preview check into `qa:mvp-readiness` as a dedicated gate, so missing WhatsApp/social-preview validation is reported separately from live shared-link write/email readiness.
- Strengthened the same public preview gate to validate deployed checkout HTML hooks for the order form, unit selector, quantity, total, register/login mode, customer identity fields, submit action, and requested/error/unavailable states before release.
- Recovered Android device readiness by booting `Pixel_3a_API_34` headlessly, confirming `emulator-5554` attached as an online ADB device, and rerunning `qa:mvp-readiness`. Android readiness now passes; the top-level gate fails only on Google OAuth readiness, shared-link live readiness, and the deployed public preview URL. The emulator was shut down cleanly after verification.
- Added `bun run --cwd apps/mobile qa:mvp-live-env-checklist` as a safe setup helper for the remaining live blockers. It reads `.env`, reports configured/missing status for Google OAuth, shared-link live write/email QA, and deployed public preview variables, prints setup hints and verification commands, and never prints configured secret values. Fixture coverage is included in `qa:mvp-contracts`.
- Added `bun run --cwd apps/mobile qa:mvp-live-readiness` as a focused non-mutating runner for the remaining external live gates. It runs Google OAuth readiness, shared-link live readiness, and deployed public preview validation together, then reports the failing live gates and points back to the setup checklist. Fixture coverage is included in `qa:mvp-contracts`.
- Added `bun run --cwd apps/mobile qa:google-oauth-live-ready` and `bun run --cwd apps/mobile qa:google-oauth-live-run` for the remaining live Google provider/API proof. The readiness gate requires a live API URL, explicit auth confirmation, fresh Google ID token, expected profile email, login/sign-up mode, and URL safety. The runner calls `auth.verifyMobileGoogle`, verifies a mobile session/profile/business context without printing the Google ID token or returned bearer token, and fixture coverage proves the command against a local mock tRPC API.
- Added `bun run --cwd apps/mobile qa:shared-link-browser-ready` and `bun run --cwd apps/mobile qa:shared-link-browser-checkout` for the remaining deployed browser checkout pass. The readiness gate requires the deployed share URL, explicit order-submission confirmation, register/login mode, disposable customer credentials, and URL safety before the Playwright runner can submit the public form and wait for the order-requested success state. Playwright is an explicit mobile dev dependency, the fixture launches Chromium against a local shared-product checkout page and verifies the submitted form payload, and `qa:mvp-live-readiness` now reports browser checkout readiness as a separate live gate.
- Aligned `bun run --cwd apps/mobile qa:mvp-readiness` with `qa:mvp-live-readiness` so the full release command also reports Google OAuth live API readiness and shared-link browser checkout readiness as first-class gates.
- Reverified Android readiness after the gate alignment by booting `Pixel_3a_API_34` headlessly. `qa:android-ready` passed, and `qa:mvp-readiness` passed source QA, focused workspace typechecks, contracts, and Android readiness before failing only on external/live configuration gates. The emulator was shut down cleanly afterward.
- Hands-on simulator or device QA is still required before this ticket can be completed: live shared-link notification delivery plus live database writes, live public browser checkout submission, live Google provider QA, and a full hands-on MVP smoke flow were not fully run.
