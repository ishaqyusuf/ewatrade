## Parent map

[Wayfinder: Mobile Design System Playground](map.md)

## Type

research

## Status

ready-for-agent

## Blocked by

None - can start immediately.

## Question

What reusable mobile primitives, token files, theme utilities, screen patterns, modals, sheets, list rows, analytics/report components, and duplicated local widgets already exist in `apps/mobile`, and which of them should influence the design-system playground?

Produce a concrete audit that names source files, current responsibilities, light/dark readiness, keyboard-safety concerns, NativeWind/style discipline risks, and whether each item should be kept canonical, folded into a better primitive, deprecated, or ignored for the playground.

## Comments

> *This was generated by AI during Wayfinder issue commenting.*

Proposed answer: Audit should treat `apps/mobile/src/components/mobile/index.ts`, `src/lib/theme.ts`, `src/styles/global.css`, `src/lib/nativewind-theme-vars.ts`, and the feature-flow primitives as the source of truth. Canonical keepers include `ActionButton`, `FormField`, `MobileScreen`, `MobileAppShell`, `StatusBadge`, `StatusBanner`, `EmptyState`, `QuantityStepper`, `OtpInput`, dashboard/report/sale/session/setup/share-link/sync primitives, and the UI primitives under `components/ui`.

Why: The app already has a richer design foundation than the map assumed. The playground should curate and harden it, not recreate it.

Assumptions / risk: Some current primitives may still be visually inconsistent; the audit should mark them as fold/rename/deprecate instead of accepting everything.
