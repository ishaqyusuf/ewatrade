## Parent map

[Wayfinder: Mobile Design System Playground](map.md)

## Type

prototype

## Status

blocked

## Blocked by

01 - Audit existing mobile primitives, tokens, and screen patterns; 02 - Decide playground information architecture and approval flow

## Question

Which exact mobile design tokens and layout rules should govern the playground and later mobile screen refactor?

Resolve color roles for light and dark mode, typography scale, spacing, radius, shadow/elevation, divider rules, card density, icon sizing, tap targets, status tones, and chart color semantics. The answer should preserve the existing Retail Ops direction while making the playground specific enough to implement.

## Comments

> *This was generated by AI during Wayfinder issue commenting.*

Proposed answer: Keep the current semantic token direction: light canvas `background`, white `card`, deep teal `primary`, green `success`, amber `warn`, red `destructive`, muted dividers, and matte dark counterparts. Standardize around compact operational density: rounded `2xl` cards/panels, full-pill controls for selectable segments, divider-based rows, 44px minimum tap targets, restrained shadows, and semantic chart colors from `chart1` through `chart5`.

Why: The existing `THEME` and NativeWind vars already encode the desired direction and match the Brain design notes.

Assumptions / risk: Typography scale still needs exact playground examples so large totals, section headers, labels, and helper copy do not drift.
