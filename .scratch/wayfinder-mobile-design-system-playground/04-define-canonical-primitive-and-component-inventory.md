## Parent map

[Wayfinder: Mobile Design System Playground](map.md)

## Type

prototype

## Status

blocked

## Blocked by

01 - Audit existing mobile primitives, tokens, and screen patterns; 03 - Lock mobile theme tokens, typography, spacing, and density rules

## Question

What canonical component inventory should the playground expose?

Define the approved mobile primitives and patterns: headers, screen containers, buttons, icon buttons, segmented controls, toggles, inputs, OTP input, search, quantity stepper, badges, banners, empty states, loading states, error states, list rows, cards, timeline rows, product rows, order rows, staff/customer rows, and action menus. Include variants, states, and naming conventions.

## Comments

> *This was generated by AI during Wayfinder issue commenting.*

Proposed answer: The playground should expose canonical primitives in tiers: foundation primitives, mobile shell/header primitives, form/action primitives, status/state primitives, row/list primitives, domain-flow primitives, and analytics/report primitives. It should also show each major state: default, pressed, selected, disabled, loading, empty, error, offline, pending, success, destructive, and keyboard-open where relevant.

Why: The current exports already form a practical component library; grouping by usage will make approval easier than a flat component dump.

Assumptions / risk: Naming cleanup may be needed later, but the playground should avoid broad renames until visual approval is complete.
