{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the EwaTrade dashboard wrapper.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
