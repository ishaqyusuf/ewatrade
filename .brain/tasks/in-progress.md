# In Progress

## Purpose
Work currently being executed.

## Items
- Mobile design-system playground implementation: reference-led approval flow is active on internal `/design-system`; the first `Pin 2 / home shell reference` has been promoted into the functional `/design-system/design-01` route group with home, reference, image, orders, stock/Add, messages, and profile paths. Only the Design 01 home screen reveals bottom tabs; child routes keep the shared shell/FABs without the tab dock. Design 01 is awaiting owner approval before the next reference image or existing-screen refactor work begins. Android emulator `emulator-5554` should stay running and be reused for future mobile validation.
- Mobile Retail Ops MVP QA and release closure: implementation-local readiness is green; production release closure remains gated by live Google OAuth, shared-link live write/email, deployed preview, and browser checkout proof values.
- Expanding the initial monorepo scaffold into full web, mobile, API, auth, and database packages.
